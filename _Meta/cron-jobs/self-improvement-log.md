# Self-Improvement Log — memory-curator

> Running record for Part F of the unified memory-management directive.
> Nightly `nightly-self-improvement-log` job appends daily observations here.
> Sunday `weekly-self-improvement-report` job reads this to produce the weekly cumulative report.

Created: 2026-08-26

### 2026-08-26 — Decision: Hindsight read-auth deferred (runbook ready)
- Live probe: `/v1/default/banks` + `/v1/default/banks/avibe-hq/stats` return **HTTP 200 without auth** (bank metadata + graph stats exposed; note content NOT exposed; writes already key-protected).
- Ole chose **option 2 (defer)** + **option 3 (runbook)** — operator-installer busy with urgent job.
- Runbook: `~/Obsidian/_Inbox/Hindsight-Read-Auth-Runbook.md` (mechanism, verify steps, client-side key placement, safety, decision log).
- Reminder: cron `hindsight-read-auth-reminder` (Mon 10:00 → Telegram) flags when to do option 1 at scale.
- Other fixes same day: wrong-bank bug fixed (operator-installer `hindsight/config.json` → `avibe-hq`); AGENT_RULES §3.1 schedule updated (18:00 → 05:00/05:15/06:00 chain).

### 2026-08-27 — 2nd PC setup + Hindsight-for-LexFlow decisions
- **2nd PC provisioning scheduled:** calendar event "2nd PC Setup plus Hindsight Test" (Home cal, Thu Sep 3 10:00-11:30) + cron `second-pc-setup-reminder` (Thu 09:00 → TG). Runbook: `~/Obsidian/_Inbox/2nd-PC-Bootstrap-Runbook.md`.
- **Calendar access:** YES via macOS AppleScript (Home/Work/etc.). Note: inline heredoc AppleScript with `&` triggers the shell backgrounding guard — write to a .applescript file and `osascript` it; `date "Thursday…"` strings are locale-fragile, use `current date` + day offsets. EventKit XPC error on some calendars — use "Home".
- **Hindsight for LexFlow (backend-dev, 2026-08-27):** use SEPARATE Hindsight service + HTTP API, NOT clone/embed. LexFlow needs its own bank. **Per-client banks confirmed correct** — backend-dev endorses (b): one bank per law firm (e.g. lexflow-clientA-hq), same single deployment, distinct bank_id per client. Rationale: hard data isolation at retrieval layer, GDPR right-to-be-forgotten = atomic `DROP bank_id=firmX`, banks are cheap namespaces (not clusters), trivial query routing (bank_id from session), per-bank auth. Ole's instinct validated.
- **Vault-logging enforcement + scaling thresholds** added (see crons).

### 2026-08-28 — Hindsight READ-AUTH ENABLED ✅ (was deferred since 08-26)
- **Fix applied via Railway CLI** (CLI was already authenticated as ole00007 — no token needed from Ole).
- Added 2 variables to service `avibe-hindsight` (project graceful-presence, ID 9b08be93-b288-4772-8ccc-29b724851bf0):
  - `HINDSIGHT_API_TENANT_EXTENSION=hindsight_api.extensions.builtin.tenant:ApiKeyTenantExtension`
  - `HINDSIGHT_API_TENANT_API_KEY` = same 64-char value as `HINDSIGHT_API_KEY` (so all existing clients already have the right key — no client-side change needed).
- Source of truth: Hindsight code `hindsight-api-slim/hindsight_api/extensions/builtin/tenant.py` — `DefaultTenantExtension` = NO auth by default; `ApiKeyTenantExtension` validates `Authorization: Bearer` against `HINDSIGHT_API_TENANT_API_KEY`.
- Env change auto-triggered redeploy (~5 min, large image). Verified:
  - `/health` → 200 (stays open for Railway probes) ✓
  - `/v1/default/banks` no-key → **401** ✓ (was 200)
  - `/v1/default/banks/avibe-hq/stats` no-key → **401** ✓ (was 200)
  - `/v1/default/banks` with key → **200** ✓
  - `/v1/default/banks` wrong key → **401** ✓
- **Note:** the auth is a SINGLE shared key (ApiKeyTenantExtension) — this closes the public-read hole. It does NOT give per-client bank isolation (that's the future `lexflow-hq` / per-client-bank work). Revisit when scaling.
- Railway CLI notes: `railway link -p graceful-presence --environment production`, then `railway service avibe-hindsight`, then `railway variables set NAME=val`, `railway redeploy`. Env-var changes auto-trigger deploy.

## Log Entries

### 2026-08-26 — Baseline (wikilinks initiative)
Vault retro-fitted into a connected graph: 210/226 notes linked (was 8/177), 409 edges.
- Total notes: 323
- Unlinked (0 links): 30 — ALL are hub/INDEX/README notes (intended: they receive backlinks)
- Distinct unresolved [[targets]]: 28 (40 refs) — mostly template placeholders (TOC, Agent-Name, Relevant-Workflow) + a few genuinely-missing notes (Composio-Integration, Email-Digest-Pipeline)
- Future weekly reports trend these numbers against this baseline.
- **Daily note:** `05-Daily/2026-08-26.md` does not exist — no staging log for today. Latest daily notes on disk: 2026-08-25, 08-24, 08-23, 08-20, 08-13.
- **(a) Conflicts flagged today:** 0 (no stager run detected).
- **(b) Identical dupes skipped:** 0 recorded.
- **(c) _Conflicts backlog size now:** N/A — `~/Obsidian/_Inbox/_Conflicts/` does not exist yet. No prior count to compare.
- **(d) Recurring pattern observed:** Yes — even without a stager run, `_Inbox/` already holds several parenthetical version-suffix collisions that would be conflict candidates:
  - `nous_models_purpose_first.xlsx` + `nous_models_purpose_first (1).xlsx`
  - `md.md` + `md (1).md`
  - `industry-competitive-analyst-skill.md` + ` (1)` + ` (2)`
  - `med_expert_editorial_plan_attached (1).md` (no base file present)
  - `image (2).jpg` (no base file present)
  Pattern: browser re-download `" (n)"` suffix. Also note serial-part families (`ica-v3-1..5`, `industry-competitive-analyst-v3-part1..3`) which are legitimate multi-part sets and must NOT be treated as dupes.
- **(e) False-positive suspicion:** `md (1).md` / `md.md` — generic base name, likely two unrelated pastes rather than a true duplicate. Also flagged: any `-part1/2/3` or `v3-N` family should be excluded from suffix-based dedup heuristics.
- **Note:** first nightly entry; baseline established.

### 2026-08-27 — Nightly log (downloads stager run)
- **Daily note:** `05-Daily/2026-08-27.md` present — downloads_stager.py executed (real, 24h window): 0 newly staged, 2 verified identical, 1 conflict quarantined, 1 system file excluded.
- **(a) Conflicts flagged today:** 1 — `md (1).md`. Vault copy (md5 6352b3f7) preserved; divergent Downloads copy (md5 771f3de3) quarantined to `_Inbox/_Conflicts/md (1).md` for review.
- **(b) Identical dupes skipped:** 2 — `olesia_state_overview.xlsx` (md5 477fd5d1, verified identical) + `hermes_tracking_plan (1).xlsx` (md5 e5c7d691, verified identical). Both correctly skipped (not staged).
- **(c) _Conflicts backlog size now:** 1 (`md (1).md`). Previous count: N/A (`~/Obsidian/_Inbox/_Conflicts/` did not exist on 2026-08-26). Net change: dir created, +1 conflict quarantined.
- **(d) Recurring pattern observed:** Yes — browser re-download ` (n)` version-suffix collisions continue. Today's instances: `hermes_tracking_plan (1).xlsx` (verified-identical, skipped) and `md (1).md` (divergent, quarantined). Same ` (1)` signature seen in the 2026-08-26 baseline (`nous_models_purpose_first (1).xlsx`, `md (1).md`, `industry-competitive-analyst-skill (1)`). Serial-part families (`v3-N`, `-part1..3`) remain legitimately distinct — still excluded from suffix dedup.
- **(e) False-positive suspicion:** None of today's flags look like false positives. `md (1).md` was the 2026-08-26 false-positive suspect (generic name, "two unrelated pastes"), but the stager proved it IS a genuine conflict — divergent checksums (771f3de3 ≠ 6352b3f7), correctly quarantined, not a false positive. `hermes_tracking_plan (1).xlsx` carries the conflict-looking ` (1)` suffix but was verified identical (md5 e5c7d691) and correctly skipped — benign re-download, not a flag error.
- **Note:** first real conflict actually quarantined into `_Conflicts/`; backlog now non-empty. The 2026-08-26 suspicion on `md (1).md` is resolved as a true positive.

### 2026-08-28 — Nightly log (downloads stager run)
- **Daily note:** `05-Daily/2026-08-28.md` present — downloads_stager.py executed (real, 24h window): 0 newly staged, 0 verified identical, 0 new conflicts, 1 system file (`.DS_Store`) excluded. No Downloads files fell inside the 24h recency window (3 most-recent items are already in `_Inbox` and just outside the cutoff).
- **(a) Conflicts flagged today:** 0 — stager ran but raised no new conflicts. (Stale conflict from 2026-08-27 remains unresolved — see note below.)
- **(b) Identical dupes skipped:** 0 recorded today.
- **(c) _Conflicts backlog size now:** 1 (`md (1).md`). Previous count (2026-08-27): 1. Net change: 0 — backlog unchanged; the single quarantined conflict has persisted un-reviewed for a 2nd consecutive day.
- **(d) Recurring pattern observed:** Yes — the browser re-download ` (n)` version-suffix collision family persists. The 3 most-recent Downloads items sit just outside the 24h cutoff and are already in `_Inbox`: `hermes_tracking_plan (1).xlsx` (verified-identical re-download, skipped 2026-08-27) and `md (1).md` (divergent conflict, still quarantined). Same ` (1)` signature as the 2026-08-26/27 baselines. Serial-part families (`v3-N`, `-part1..3`) remain legitimately distinct — excluded from suffix dedup.
- **(e) False-positive suspicion:** None new. `md (1).md` remains a confirmed TRUE conflict (divergent md5 771f3de3 ≠ 6352b3f7) — was the 2026-08-26 false-positive suspect, confirmed true positive 2026-08-27, and still unresolved as of today. No new flag today to suspect.
- **Note:** 2nd consecutive day with no new conflicts and one unresolved stale conflict. `_Inbox` backlog now 237 files (growing, not being triaged out into `01-Projects`/`03-Resources`). Recommended: human review of `md (1).md` (keep vault / accept Downloads / merge) + periodic `_Inbox` triage pass (see `downloads_cleanup.py`).

### 2026-08-29 — Nightly log (NO stager run detected)
- **Daily note:** `05-Daily/2026-08-29.md` does NOT exist — no downloads_stager.py run captured for today (no [STAGED]/[CONFLICT]/dedup lines to read).
- **(a) Conflicts flagged today:** 0 (no stager run detected).
- **(b) Identical dupes skipped:** 0 recorded.
- **(c) _Conflicts backlog size now:** 0. Previous count (2026-08-28): 1. **Net change: -1** — the single long-lived `md (1).md` conflict (quarantined 2026-08-27, unresolved 2026-08-27 and 2026-08-28) is GONE. Global vault search for `md (1)` returns 0 hits, so it was **deleted/removed wholesale, not merged or relocated** into the vault. Backlog now empty.
- **(d) Recurring pattern observed:** No new instance today (no stager run). The browser re-download ` (n)` version-suffix collision family remains the dominant recurring signature historically (seen 2026-08-26/27/28: `nous_models_purpose_first (1).xlsx`, `md (1).md`, `industry-competitive-analyst-skill (1)`, `hermes_tracking_plan (1).xlsx`, `image (2).jpg`). Serial-part families (`v3-N`, `-part1..3`) remain legitimately distinct and excluded from suffix dedup.
- **(e) False-positive suspicion:** None new. **Flag for Ole:** the previously-confirmed TRUE conflict `md (1).md` (divergent md5 771f3de3 ≠ 6352b3f7) vanished from `_Conflicts/` with no merge record anywhere in the vault — its removal looks like a hard delete rather than an intentional resolution. Confirm intent (was the divergence reviewed and the right copy kept?) before assuming it was correctly cleared.
- **Note:** 1st day of empty `_Conflicts/` backlog since the directory was created (2026-08-27). Stale-conflict watch: resolved. Outstanding: `_Inbox` triage (237 files) and the recurring ` (n)` re-download pattern prevention still unaddressed.

## Links
- Parent: [[cron-jobs-INDEX]]

## WEEKLY ENFORCEMENT — 2026-08-30 (vault-logging, §V3/V4)
- Profiles checked: **23** (all profiles under ~/.hermes/profiles/)
- Left a vault trace this week: **23 / 23** ✓
- Non-compliant (active but zero vault trace): **none**
- Session-active this week: 18 profiles; idle: chatbot_builder, content-creator, sales-crm, seo-aeo-expert, telegram-utility-agent
- Thin-trace watchlist (1–3 notes touched in 7d, mostly cron one-liners): chatbot_builder (1), chatseo-agent (2), crm-outreach-agent (2), gsc-agent (2), seo-cron-agent (2), seo-swarm-agent (2), telegram-utility-agent (2), email-digest-agent (3), frontend-developer-lovable_react (3)
- Daily-note gaps (last 7d): **2026-08-26 missing**; 08-27/08-28/08-30 are thin (18–22 lines)
- Action: remind thin-trace cron agents to append a one-line Daily Note entry per run per §V3.

## WEEKLY REPORT — 2026-08-30 (Part F synthesis)
- Window: 2026-08-26 → 2026-08-30 (first full week of the wikilinks initiative + vault-logging enforcement).
- **(1) FALSE-POSITIVE RATE**
  - Nightly `downloads_stager.py`: **0%** false positives. Only 1 conflict ever quarantined (`md (1).md`) — confirmed TRUE (divergent md5 771f3de3 ≠ 6352b3f7). ` (1)`-suffixed identical re-downloads correctly skipped (benign).
  - `sunday-organize-inbox` (18:15 today): dumped **45 files** into `_Conflicts/`. ≥12 are serial-part families (ica-v3-1..5, industry-competitive-analyst-v3-part1..3, Point1-5) — LEGITIMATELY DISTINCT per AGENT_RULES §3/§7 and must NOT be quarantined → **~27% false-positive rate** on this batch. 3 are confirmed TRUE divergent duplicates (`Deep_Research_Report… (1)`, `Point6… (1)`, `industry-competitive-analyst-skill (2)` — all md5-DIFFER from base). ~30 others pending manual review.
- **(2) RECURRING PATTERNS** — browser re-download ` (n)` suffix collisions remain the dominant weekly signature. Serial-part families (v3-N / partN / PointN) repeatedly mis-flagged as would-be dupes. **Proposed rule refinement → see (6).**
- **(3) BACKLOG TREND** — `_Conflicts/` size: 08-26 N/A → 08-27:1 → 08-28:1 → 08-29:0 → **08-30:45**. Net week-over-week 0 → 45 (spike from today's organize-inbox run). The long-lived `md (1).md` was hard-deleted 08-29 with no merge/relocate record (flagged for Ole's confirmation 08-29).
- **(4) WIKILINKS HEALTH** (regression vs 2026-08-26 baseline): Notes 323→339 (+16). Zero-link 30→56 (+26); of the 56, **6 are genuine orphan CONTENT notes** (flagged: `Untitled.md`, `Hermes doctor 29 08 2026.md`, `2026-08-29.md`, `Hindsight-Read-Auth-Runbook.md`, `2nd-PC-Bootstrap-Runbook.md`, `LexFlow-CRM-Architecture.md`). Unresolved distinct targets 28→41 (+13); 59 refs (baseline 40). New unresolved dominated by template placeholders (`[[TOC]]`, `[[Agent-Name]]`, `[[<sibling note>]]`) + still-missing `Composio-Integration`, `Email-Digest-Pipeline`, `avibe-hindsight`, `LexFlow`.
- **(5) VAULT-LOGGING COMPLIANCE** (weekly enforcer 18:12): **23/23 profiles left a vault trace ✓, 0 non-compliant.** Idle (no trace expected): chatbot_builder, content-creator, sales-crm, seo-aeo-expert, telegram-utility-agent. Thin-trace watchlist (1–3 notes/7d): chatbot_builder(1), chatseo-agent(2), crm-outreach-agent(2), gsc-agent(2), seo-cron-agent(2), seo-swarm-agent(2), telegram-utility-agent(2), email-digest-agent(3), frontend-developer-lovable_react(3). Daily-note gaps: **2026-08-26 MISSING**; 08-27/08-28/08-30 thin (18–22 lines).
- **(6) PROPOSED ADJUSTMENT (awaiting Ole's decision):** Harden `sunday-organize-inbox.py` + `downloads_stager.py` conflict detection with an explicit **serial-part EXCLUDE rule** — filenames matching `(?:v\d+-\d+|-part\d+|part\d+|point\d+-|_v\d+)` skip quarantine and are treated as distinct notes (optionally grouped under a hub). This would have prevented the ≥12 false-positive quarantines in today's 45-file `_Conflicts/` backlog.

### 2026-08-30 — Nightly log (NO stager run; large `_Conflicts` spike from organize-inbox)
- **Daily note:** `05-Daily/2026-08-30.md` present but contains **no [STAGED] / [CONFLICT] / dedup lines** — `downloads_stager.py` did NOT run (or logged nothing). Note content is the 07:30 lexflow morning briefing + the 12:5x Hindsight free-only fallback fix.
- **(a) Conflicts flagged today:** 0 from the nightly stager. However **45 files** now sit in `_Inbox/_Conflicts/` following today's 18:15 `sunday-organize-inbox` run (that job, not the stager, produced them).
- **(b) Identical dupes skipped:** 0 recorded (no stager run → no md5 verify pass logged).
- **(c) _Conflicts backlog size now:** **45**. Previous count (2026-08-29): 0. **Net change: +45** — largest single-day spike since the directory was created (2026-08-27). Trend: N/A → 1 → 1 → 0 → **45**.
- **(d) Recurring pattern observed:** Yes, two distinct families in today's 45:
  1. Browser re-download ` (n)` suffix — `Deep_Research_Report_Car_Industry_Skill_Session (1).md`, `Point6_Skill_MCP_Marketplaces_Extended (1).md`, `industry-competitive-analyst-skill (2).md`. Same dominant weekly signature as 08-26/27/28.
  2. **Serial-part families mis-quarantined** — `ica-v3-1..5`, `industry-competitive-analyst-v3-part1..3`, `Point1..Point6`, `dev_landing_page_RU/EN` (language pair). These are legitimately DISTINCT notes, not duplicates.
  3. Generic/low-signal base names also quarantined: `md.md`, `INDEX.md`, plus several prompt-fragment titles ("1. Make a step by step instruction…", "find in this thread existing vertical legal saas a…") which are truncated-prompt filenames, another recurring source of collisions.
- **(e) False-positive suspicion:** **HIGH — ≥12 of 45 (~27%)**. All `v3-N` / `-partN` / `PointN` members and the RU/EN language pair look like true false positives (distinct content, suffix-only similarity). `INDEX.md` is especially concerning — quarantining a hub note breaks wikilink graph parents (§V2). Confirmed TRUE conflicts among the 45: the 3 md5-divergent ` (n)` items listed in (d).1. Remaining ~30 pending manual review.
- **Note:** the nightly stager produced a clean record for the 3rd time in 5 days, but `sunday-organize-inbox` is now the main source of noise. The serial-part EXCLUDE rule proposed in the 2026-08-30 weekly report §(6) remains the single highest-value fix; recommend also excluding `INDEX.md`/hub notes from quarantine unconditionally.

## Links
- Parent: [[_Meta/cron-jobs/INDEX]]
- Related: [[_Meta/AGENT_RULES]]

### 2026-09-05 — Nightly log (NO stager run; no daily note; `_Conflicts` backlog grew to 46)
- **Daily note:** `05-Daily/2026-09-05.md` does **NOT exist** — no downloads_stager.py run captured today (no [STAGED]/[CONFLICT]/dedup lines to read). The latest existing daily note is `2026-09-04.md` (yesterday's lexflow briefing). No staging activity to log from the stager itself.
- **(a) Conflicts flagged today:** 0 from the nightly stager. However, `_Inbox/_Conflicts/` was touched at **05:00 today** (Sep 5) — 3 files landed there at exactly the same minute (`Screenshot_2026-08-30_at_15.45.46.png`, `Screenshot_2026-09-04_at_13.10.08.png`, `LexFlux — Legal SaaS UX UI Design Research Report.md`). The identical 05:00 mtime + same batch suggests a scheduled job (likely `sunday-organize-inbox` or a sibling organizer) ran today and dropped these as conflict candidates. Without a [CONFLICT] line in a daily note, the conflict-detection source is inferred from filesystem metadata, not from explicit stager logging.
- **(b) Identical dupes skipped:** 0 recorded (no stager run with a dedup log).
- **(c) _Conflicts backlog size now:** **46**. Previous count (2026-08-30): 45. **Net change: +1** — but mtime inspection shows **4 net additions** since 08-30: the 3 files at Sep 5 05:00 (above) + `LexFlow_Agentic_Roadmap.json` (Sep 3 12:49) — so the **-3** delta vs the reported +1 must come from intervening deletions (likely Ole manually reviewed and cleared some of the 2026-08-30 quarantine batch during the week; the `md (1).md` long-lived conflict already gone per 08-29 entry, and the `industry-competitive-analyst-skill (1)/(2)` etc. were likely human-triaged). Trend: N/A → 1 → 1 → 0 → 45 → **46**.
- **(d) Recurring pattern observed:** Yes, two distinct signatures continue:
  1. **Browser re-download ` (n)` suffix family** — historical baseline (08-26 → 08-30). Today's Sep 5 batch contains `LexFlux — Legal SaaS UX UI Design Research Report.md` (no ` (n)` suffix, but **name clash with the un-quarantined `LexFlow`/`LexFlow_Agentic_Roadmap.json` family** — em-dash vs underscore + "Legal SaaS" modifier distinguishes it, but the **near-name** is a new false-positive trap: any "Lex" + Flow/Flux variant now collides lexically).
  2. **Screenshot capture pattern** — 2 of the 3 new files today are `Screenshot_YYYY-MM-DD_at_HH.MM.SS.png`. This is a NEW recurring pattern (not seen in prior logs): automated/manual screenshot capture dumped to Downloads and routed to `_Conflicts/` by the organizer. These are probably NOT real conflicts — they're date-stamped captures that happen to share the same destination. Flag for the proposed serial-part / capture-pattern EXCLUDE rule.
  3. Serial-part families (`ica-v3-1..5`, `-partN`, `PointN`) remain legitimately distinct and excluded from suffix dedup (per 2026-08-30 weekly §(6) proposal, still un-implemented).
- **(e) False-positive suspicion:** **HIGH for the Sep 5 batch.** `Screenshot_*.png` files are timestamped, not duplicated — almost certainly false positives unless two captures share the exact same second (unlikely). `LexFlux — Legal SaaS UX UI Design Research Report.md` is a distinct research report (em-dash title, "Legal SaaS" qualifier) and likely NOT a duplicate of `LexFlow_*` — separate project research artifact. **All 3 of today's Sep 5 additions are likely false positives.** The `LexFlow_Agentic_Roadmap.json` from Sep 3 (json, not md; also legitimately distinct roadmap artifact) is the most plausible true positive among the 4 new files — but no md5 verify was performed because the stager didn't run with a logged dedup pass.
- **Note:** 2nd consecutive day with no nightly stager run (08-29 + 09-05). The `_Conflicts/` backlog is being mutated by a DIFFERENT job (organizer, ~05:00 cron) without [STAGED]/[CONFLICT] daily-note markers, so the nightly log can only infer changes from filesystem mtime. Recommended: (1) require `sunday-organize-inbox` (and any 05:00 organizer job) to append a [CONFLICT] line to the daily note just like `downloads_stager.py` does; (2) extend the proposed §(6) EXCLUDE rule to cover `Screenshot_*` patterns + em-dash/underscore variant names + timestamped captures. No human review of the new files recorded in this log entry — they sit un-triaged.

### 2026-09-16 — Nightly log (stager RAN; 5 new quarantines; backlog 46 → 51; both producer jobs finally identified)
- **Daily note:** `05-Daily/2026-09-16.md` exists but carries **no `[STAGED]` / `[CONFLICT]` lines** — the marker lines are written to `~/.hermes/profiles/operator-installer/logs/staging-YYYYMMDD.log`, **not** the daily note. From today the nightly job reads that log dir directly (plus `dedup-YYYYMMDD.log`); filesystem-mtime inference (used 09-05) is retired.
- **Producers identified (unnamed in every earlier entry):** `obsidian-staging.sh` (cron job `obsidian-daily-staging` `9eb2b7e6fb4b`, `0 5 * * *`) is the real Downloads→`_Inbox` stager; `obsidian-dedup-check.sh` (cron `obsidian-dedup-check` `e7f5481a9798`, `15 5 * * *`) is the `_Inbox`→`_Conflicts` sweeper. `memory-curator`'s own `downloads_stager.py` is a **different** script and did not run today (TCC denial on `~/Downloads`, logged 22:00 in the daily note) — so "no stager run" and "the stager ran" are both true, for two different stagers. Doc drift noted: `obsidian-staging.sh` header says "daily at 18:00 via launchd" while the live schedule is 05:00 Hermes cron.
- **(a) Conflicts flagged today: 5.** — 4 by staging @05:12:04, all logged `[DUPLICATE] … already in inbox` (basename collision with `_Inbox`): `Instance-Capacity-Runningcost-Stoppedcost-Approx30.csv`, `LexFlow_Automation_MasterPlan.xlsx`, `Dimension-LocalHermes-HermesCloud.csv`, `LexFlow_Rollout_Tracker.xlsx`. — 1 by dedup @05:29:29, logged `[FLAGGED] --.csv — different size than 03-Resources/Reference-General/--.csv`. **0 `[CONFLICT]` lines** (no Downloads basename matched a `01-Projects`/`02-Areas` file). Same run staged **15** files to `_Inbox`.
- **(b) Identical dupes skipped: 0 by the tooling.** Neither script compares content — staging keys on **basename + existence only**, dedup on **size only**. Independent md5 check of today's 5: **2 are byte-identical to their `_Inbox` twin** and were quarantined needlessly — `Instance-Capacity-Runningcost-Stoppedcost-Approx30.csv` (c56e8464 = c56e8464), `Dimension-LocalHermes-HermesCloud.csv` (fe10b01e = fe10b01e). `LexFlow_Automation_MasterPlan.xlsx` (c8bfbdea) is **identical to the `01-Projects/LexFlow/` copy** but ≠ the stale `_Inbox` copy (c3ef3c86) → the quarantine is arguably right, the `_Inbox` copy is the stale one. `LexFlow_Rollout_Tracker.xlsx` (73d82255 ≠ 9c33a9fa) → genuine divergence, true positive. `--.csv` (254 B) unverified.
- **(c) `_Conflicts` backlog now: 51 files / 560 KB.** Previous logged count (2026-09-05): 46. **Net +5**, fully reconciled against git rather than mtimes: the **46** files committed at `ec783f7` (2026-09-05 22:31 auto-sync) are **all still present — 0 removals** — plus 5 untracked new arrivals (4 × 2026-09-16 05:12, 1 × `--.csv`, mtime preserved from its 09-15 05:15 staging).
- **(d) Recurring pattern observed (yes, three live families):**
  1. **` (n)` browser re-download collision — still dominant:** `LexFlow_Ecosystem_Index (1).xlsx` was staged in the *same run* as `LexFlow_Ecosystem_Index.xlsx`, and `LexFlow_Automation_MasterPlan (1).xlsx` was staged while the base name was being quarantined. Same signature as 08-26/27/28.
  2. **Repeat-offender name — `LexFlow_Automation_MasterPlan.xlsx`:** deleted as a duplicate on 09-04 (copy still in `_Trash/_Deleted-Dupes-20260904_222514/_Conflicts/`), re-downloaded and re-quarantined today, and **3 distinct contents now live under this one name** (`_Conflicts`+`01-Projects` / `_Inbox` / `_Inbox (1)`). Highest-value single item for human triage.
  3. **Degenerate / mangled filenames:** `--.csv` (254 B, a shell- or export-mangled name) quarantined on a size mismatch alone; joins `md.md`, `INDEX.md`, `--.csv`-class junk already in the backlog.
  4. **Bulk sheet-export batches:** the 05:12 run was 15 files, mostly workbook→CSV exports with generic sheet names (`Roster.csv`, `Decisions_Log.csv`, `Delegation_Tracking.csv`, `Sheet-Mergedcontent.csv`, `hermes-cloud-plan.csv`, `Elisa_Access_Plan.csv`, `Term-Simplemeaning-Whyitmatterstoyou.csv`) — a systematic future collision source, not an accident.
  5. Serial-part families (`v3-N`, `-partN`, `PointN`, RU/EN language pairs) unchanged, no new members today; still legitimately distinct and excluded from suffix dedup.
- **(e) False-positive suspicion: HIGH — 2 of 5 (40%) provably identical**, and the structural risk is worse than the count: (i) staging quarantines on basename+existence, so a byte-identical re-download is filed as a "duplicate" instead of skipped; (ii) **dedup `rm`s inbox files on size equality alone** and labels them "exact duplicate" — same size ≠ same bytes, so this path can silently destroy a divergent file with no backup. Both are the highest-risk behaviours this job has surfaced to date.
- **Correction to the 2026-09-05 entry** (archive-never-delete: that text is left intact above; this supersedes it): its claim that `Screenshot_2026-08-30_at_15.45.46.png`, `Screenshot_2026-09-04_at_13.10.08.png` and `LexFlux — Legal SaaS UX UI Design Research Report.md` "landed in `_Conflicts/`" is **wrong** — all three sit in `_Inbox/` untouched, and `git ls-tree ec783f7 -- _Inbox/_Conflicts` matches **0** screenshots. Its 09-05 total (46) was correct, but its content list was inferred from mtimes and mis-attributed. Its "+1 vs 4 net additions ⇒ intervening deletions" reasoning is superseded: there were **no** deletions; the tracked set was unchanged between 08-30 and 09-05.
- **Recommendations (proposal only — nothing wired, per "share here first"):** (1) have both scripts append a one-line `[STAGED] n / [DUPLICATE] n / [FLAGGED] n` summary to that day's `05-Daily` note so §V3 is satisfied and Part F needs no filesystem archaeology — alternatively formally bless this job reading `~/.hermes/profiles/operator-installer/logs/`, which is what it did today; (2) replace dedup's size-only `rm` with an md5 check (or move to `_Trash`, never delete); (3) make staging skip quarantine when the candidate is byte-identical; (4) triage the 51-file backlog, starting with the `LexFlow_Automation_MasterPlan.xlsx` name and the sheet-export pairs (`LexFlow_Rollout_Tracker`, `Dimension-*`, `Instance-*`, `--.csv`).
- **Note:** no user-facing report from this run (Part F logs only; the Sunday `weekly-self-improvement-report` job consumes this file).

## Links
- Parent: [[_Meta/cron-jobs/INDEX]]
- Related: [[_Meta/AGENT_RULES]]
- Related: [[downloads_stager]]
- Related: [[organize_inbox]]

### 2026-09-18 — Nightly log (stager GREEN after TCC outage; 0 conflicts; 3 identical dupes archived; backlog reconciled to 49)

- **Sources read (producer split, per 09-16 correction):** `~/.hermes/profiles/memory-curator/logs/downloads_stager_2026-09-18.json` (memory-curator `downloads_stager.py`, 18:00 cron) + `~/.hermes/profiles/operator-installer/logs/{staging,dedup}-20260918.log` (the 05:00/05:15 chain). Daily note `05-Daily/2026-09-18.md` exists and carries the stager summary in prose (section "Downloads → `_Inbox` stager (memory-curator, 18:00 cron)").
- **(a) Conflicts flagged today: 0.** — `downloads_stager.py` ran **green** twice: 18:00:24 (24 h window) = 10 staged, 0 identical-skipped, **0 conflicts**, 0 errors (only `.DS_Store` excluded); 18:02:54 (`--hours 72` backfill for the 09-16→09-18 blind window) = 8 staged, **29 md5-verified identical skipped**, **0 conflicts**, 0 errors. — `obsidian-dedup-check.sh` @05:18:35 = `0 moved to _Conflicts, 0 flagged as different`. — `obsidian-staging.sh` @05:12:35 **FAILED again**: `find: ~/Downloads: Interrupted system call` (same failure mode as the 09-17 `PermissionError [Errno 1]`; the 05:00 chain is still broken while the 18:00 stager works — see incident note from 09-17).
- **(b) Identical dupes skipped: 29 by the stager + 3 removed vault-side.** — Stager's md5 skip list is the largest ever logged (29 names, incl. `hermes-install-and-safety-prompt (2).md`, `tutor-me-learning-plan (1).xlsx`, `2026-09-15-hermes-multi-agent-ops-plan-v2.md`, `Untitled design.png`). — Vault-side dedupe (archive-only) moved **3 byte-identical `(1)`-suffix groups** to `_Trash/_Deleted-Dupes-20260918_180348/_Inbox/`. **Independently md5-checked by this job — all 3 confirmed identical to their base copy:** `-bestshotever (1).csv` = `-bestshotever.csv` (**e0cc0507**), `CANDIDATE-full-stack-rules-v0.2 (1).md` = base (**022177f3**), `hermes-cloud-plan (1).csv` = base (**2634759c**). Archive-not-delete honoured; nothing destroyed.
- **(c) `_Conflicts` backlog now: 49 files / 556 KB**, all git-tracked, working tree clean (no uncommitted deltas). **Reconciled against git, not mtimes:** tracked counts are 08-30 `693f797` = 45 → 09-05 `32f710b` = 46 → 09-16 `3b4c053` = 48 → HEAD `7565b00` = **49**. **Net since the last logged entry: +1** — the only tracked arrival is `LexFlow_Automation_MasterPlan.xlsx` (the repeat-offender named 09-16, unchanged mtime 09-03 13:06). Nothing was removed from `_Conflicts` in that span. 0 new quarantines today.
- **(d) Recurring patterns observed (four, one new):**
  1. **` (n)` re-download family — now the top source of BOTH conflicts and identical dupes:** today's instances `-bestshotever (1).csv` **staged in the same run as its own base `-bestshotever.csv`**, `hermes-cloud-plan (1).csv`, `CANDIDATE-full-stack-rules-v0.2 (1).md`, `hermes-install-and-safety-prompt (2).md`, `tutor-me-learning-plan (1).xlsx`. Signature unchanged since 08-26.
  2. **NEW, and the highest-risk trend: degenerate dash-mangled filenames.** The family grew from 1 to **4 distinct names in a single run** — `-.csv`, `--Cost-.csv`, `-Question-Alreadyansweredbytheplan.csv` (all staged/fingerprinted today) plus the long-resident `--.csv` (254 B). Systematic cause: workbook→CSV sheet exports whose sheet title begins with a dash/em-dash, so the filename collapses to leading-dash noise. Near-identical leading-dash names are unreadable and guarantee future basename collisions.
  3. **Version-suffix families (`v2`/`v3`/`partN`) must stay excluded from suffix dedup:** today's members `lexflow_agentic_plan_v3.xlsx`, `parallel_backlog_v2_compressed.csv`, `roadmap_overview_v2.png`, `lexflow-agentic-v3.zip`, and `hermes-multi-agent-ops-plan.xlsx` vs `2026-09-15-hermes-multi-agent-ops-plan-v2.md`. Legitimately distinct content. The EXCLUDE rule proposed 2026-08-30 §(6) is **still un-implemented 19 days later** — it remains the single highest-value fix, and it now needs a dash-mangled clause too.
  4. **Mass re-download churn:** the 72 h backfill had to md5-verify and skip 29 files — i.e. more than half the window's candidates were byte-identical re-downloads. Prevention (dedupe at capture/browser level) is still unaddressed.
- **(e) False-positive suspicion:** — **LOW for today's 3 archive moves: 0% false positive** — all three are provably byte-identical (md5s above), correctly deduped, correctly archived rather than deleted. Cleanest dedup result this job has logged. — **But flag one structural false NEGATIVE:** `-bestshotever (1).csv` + `-bestshotever.csv` were staged into `_Inbox` in the same run instead of the second being skipped — the stager's identical-check keys on basename+existence within the window, so a same-run pair slips through and the vault-side dedupe has to clean it up afterwards. (Same class as 09-16's "quarantine a byte-identical re-download" finding.) — No conflict flags today, so no conflict false positives to suspect; note that the 05:00 chain still has **no md5 guard** (dedup compares size only, per 09-16) — un-remediated.
- **Correction to the 2026-09-16 entry** (archive-never-delete: that text is left intact above; this supersedes its counts): its **backlog figure of 51 was an overcount** — git shows only **48** tracked files at its own commit `3b4c053`, so the true 09-16 value is 48 (not 51), and today's 49 is **+1**, not −2. Two of the five files it listed as new (`Instance-Capacity-Runningcost-Stoppedcost-Approx30.csv`, `Dimension-LocalHermes-HermesCloud.csv`) **never persisted in `_Conflicts`**: they were removed mid-session into `_Trash/_Deleted-Dupes-20260916_222551/_Conflicts/` and their base copies live in `_Inbox/` — i.e. 09-16's own "byte-identical, quarantined needlessly" finding was remediated same day by archive-to-`_Trash`, not by deletion. (Side artefact: commit `849c9de`, a second 22:38 auto-sync that is **not an ancestor of HEAD**, is what added those two paths — worth knowing when reconciling future counts.)
- **Note:** first green stager day since the 09-17 TCC outage; first day since 08-29 with an empty new-conflict count AND a clean, fully git-reconciled backlog delta. `_Inbox` ends at 99 files (daily note recorded 98). Still open, all previously logged and none newly wired (§ "share here first"): (1) EXCLUDE rule for serial-part + `Screenshot_*` + dash-mangled names; (2) replace the 05:00 dedup size-only `rm` with md5/`_Trash`; (3) make staging skip quarantine when byte-identical; (4) fix `obsidian-staging.sh` on the 05:00 chain; (5) triage the 49-file backlog, starting with `LexFlow_Automation_MasterPlan.xlsx` and the four dash-mangled CSVs. No user-facing report from this run — Part F logs only; the Sunday `weekly-self-improvement-report` job consumes this file.
