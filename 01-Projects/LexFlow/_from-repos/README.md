# _from-repos — Provenance

These are **copies** of knowledge/doc content migrated from local code repos
into the Obsidian vault (per the architecture map, layer separation:

- Layer 1: local code repos = source of truth for CODE (stay where they are)
- Layer 3: Obsidian vault = source of truth for KNOWLEDGE)

Originals were **not moved or deleted** — they remain in the source repos.

## Sources

| Repo (source of truth for code) | Copied into vault |
|---|---|
| `~/LexFlow-landing` | `01-Projects/LexFlow/_from-repos/LexFlow-landing/` |
| `~/LexFlow-Chatbot` | `01-Projects/LexFlow/_from-repos/LexFlow-Chatbot/` |
| `~/aLEXy` | `01-Projects/aLEXy/_from-repos/` |

## What was copied

Only knowledge/doc content: `README.md`, architecture/status `.md` files,
build prompts, session handoffs, and schema/decision `.csv`/`.docx` docs.
Explicitly **excluded**: `.git/`, `node_modules/`, `.venv*`, `dist/`, `build/`,
lockfiles, and binary/dependency folders.

Migrated: 2026-08-25 · by memory-curator
