---
title: Daily Note 2026-09-05
created: 2026-09-05
tags: [daily-notes, repo-sync, security, vault]
status: active
---

# Daily Note - 2026-09-05

## Nightly repo→vault sync + auto-push (22:30 cron — memory-curator)
- **Repo sync:** ✅ clean — 0 updated, 0 new, 81 identical across all 5 repos (lexflow-crm, LexFlow-landing, LexFlow-Chatbot, aLEXy, avibe-hindsight).
- **Auto-push:** ❌ BLOCKED by GitHub push-protection (GH013). Local commit `ec783f7` made, but `git push origin main` rejected.
  - **Blocker:** commit `7cf4e6d` (2026-09-04 cron run) staged `client_secret_201026737817-...apps.googleusercontent.com.json` containing a live Google OAuth `client_secret` (project `modular-sign-507311-p7`).
  - **Local secret-guard missed it:** `git_autocommit.sh` regex only checks `api-keys|ORACLE_VPS_CREDENTIALS|debug_token|\.db$|\.env$` — does not match `googleusercontent` filenames.
  - **Root cause:** two copies of the OAuth credential file still exist in the working tree at `_Inbox/` and `_Trash/_Deleted-Dupes-20260902_222529/_Conflicts/` — the cron picked them up via `git add -A`.
- **Action needed (Ole):**
  1. **Revoke the OAuth client secret** in Google Cloud Console (`modular-sign-507311-p7` → Credentials → `201026737817-...` → regenerate `client_secret`). It is now considered leaked regardless of history-rewrite outcome.
  2. Delete the two local copies of the file from `~/Obsidian/`.
  3. Strip the file from commit history (`git filter-repo --invert-paths --path ...` or BFG), force-push.
  4. Patch `git_autocommit.sh` regex to also block `googleusercontent|client_secret|oauth|credentials|\.json$` patterns before next cron run.
- See: [[vault-push-blocked-gh013-2026-09-05]] (alert in `_Inbox/`).

## Links
- Parent: [[LexFlow-INDEX]]
- Related: [[vault-push-blocked-gh013-2026-09-05]]