# 2026-09-21

## Daily LexFlow project-status run (operator-installer, 09:00 cron)

- Ran the standing 2026-09-17 daily status job. Canonical tracker updated in place: `docs/LexFlow_Agentic_Roadmap.json` → `last_updated: 2026-09-21` + refreshed `last_status_run` evidence block (added a stable `kanban_card_map` too, so the T-00x → card-id mapping no longer lives only in prose).
- **Nothing moved today** (Monday, first run of the week): no CRM commit since `d8c9fde` (2026-09-20 docs status commit), no code commit since `6ef25da` (2026-09-17), no deploy, no push, no Kanban card activity (`kanban.db` mtime still 2026-09-19 19:45). `find` over the working tree: the only file touched in the last 36h is the roadmap JSON itself. Prod `https://web-production-031a6.up.railway.app/health` = **200** (`db: ok`, `environment: production`).
- Committed locally only: `404247e` on `lexflow_hermes_v1` — **not pushed** (a push triggers a Railway deploy, gated on Ole's explicit go). Remote branch still at `8c17456`.
- Kanban: re-read the board (51 cards). Every one of the 18 unfinished roadmap tasks still has exactly one open card → **0 new cards created** (no duplicate registry). Mapping unchanged: T-002 `t_a5f4dd68` · T-003 `t_a7e05e2c` · T-004 `t_34e5415d` · T-005 `t_cba427b1` · T-006 `t_ba95cc42` · T-008 `t_6cf123af` · T-009 `t_7bdb5c53` · T-010 `t_da4552e5` · T-011 `t_226f9045` · T-012 `t_b8dfc0cc` · T-013 `t_5cf7a308` · T-014 `t_594fcced` · T-015 `t_f0a8b939` · T-017 `t_4af8abb6` · T-018 `t_f48b0a11` · T-019 `t_bc68191b` · T-020 `t_a4e0ad76` · T-021 `t_f0f709f5`.
- Refreshed the human-readable mirror [[LexFlow-Project-Status]] (snapshot → 2026-09-21).
- WARN (new, data quality): T-001 and T-007 are `column: done` / `deploy_status: deployed` but carry no `prod_url`/`commit_hash`, which the roadmap file's own `report_rule` requires; T-016 names commit `da19b36` in notes but has no `prod_url`. Left unfilled rather than guessed — needs Ole/backend-dev to supply the hashes.
- WARN (carried, still open): `01-Projects/LexFlow/_from-repos/LexFlow-CRM/docs/LexFlow_Agentic_Roadmap.json` remains a **parallel copy** of the live tracker (now further diverged, since the live file moved on). Authority = the live repo file; the mirror is derived and should be labelled as such or removed. Nothing written back to it.
- WARN (carried): legacy July-2026 status notes `01-Projects/AVibe-CRM/LexFlow/PROJECT_STATUS.md`, `.../DEPLOYMENT_STATUS.md`, `01-Projects/aLEXy/_from-repos/PROJECT_STATUS.md` can still read as competing LexFlow status registries.
- WARN (carried): CRM repo still at the legacy `~/Desktop/projects/services/LEGAL/LEXFLOW Production/lexflow-crm` path — §8 Repo Root Rule migration staged, not yet run for LEGAL.

## memory-curator · Hindsight read-auth scaling reminder (cron)

- **Read endpoints are NOT open any more.** Probe 2026-09-21: `/v1/default/banks` and `/v1/default/banks/avibe-hq/stats` both return **401 without a key**; the same calls with `Authorization: Bearer <key>` return **200**. Key-based reads still work end-to-end (`hindsight_recall` returned 30 facts this run). The open-read state recorded in the 2026-08-26 baseline is gone — same result as the 2026-08-31 probe. The runbook's `status: pending` is therefore **stale** — the exposure it describes is closed, but nobody has confirmed whether that was the option-1 fix or came from another layer. See [[Hindsight-Read-Auth-Runbook]].
- **Trigger A breached (facts):** avibe-hq = **15,267 facts** (threshold 15,000, was ~8.9K on 2026-08-26) / **500 documents** (threshold 1,000, was 277). 645,709 links; fact types: world 9,088 · observation 4,662 · experience 1,517.
- **Trigger C likely fired (client PII):** the bank is no longer research/notes only — keyed recall surfaces client-associated PII: Alena Krot clinic addresses + phone `+39 345 8302531` across 4 locations, Romanelli test credentials (`preview@romanelli.test` / `Romanelli0826`), client sub-tenant emails (`cl1@…`, `cl2@…`), workspace IDs ws7–ws12.
- **Trigger B not met:** 6 client/service folders under `01-Projects/` (Alena-Krot-Med-Expert, Avvocato-Pagliano, Carrozzeria-2DI, Commercialista-Client, Studio-Romanelli, AVibe-CRM) vs threshold 20.
- Trigger D (2nd device) and E (hard review 2027-02-26) — no evidence of either.
- Hygiene WARN (new): `/stats` reports **174 failed operations** and **1,881 failed consolidations** (`last_consolidated_at` 2026-09-21T07:13Z). Worth a look by operator-installer; not touched this run (read-only probe, no deployment change).
- Path correction: the runbook lives at `03-Resources/Hermes-Setup-and-MCP/Hindsight-Read-Auth-Runbook.md` — the reminder job still points at `_Inbox/`, where no copy exists.

## memory-curator · avibe-hindsight network-egress & internal-networking audit (Ole request)

- Audited Railway project `graceful-presence` (services `avibe-hindsight` + `Postgres`; **no Hermes service present**) read-only via CLI + GraphQL + deploy/network/DNS logs + `metrics --network --raw` + `usage`. Full note: [[2026-09-21-avibe-hindsight-network-egress-audit]].
- **Inter-service networking is already fully private.** `HINDSIGHT_API_DATABASE_URL` and Postgres `DATABASE_URL`/`PGHOST` all point at `postgres.railway.internal`; DNS logs resolve only internals (zone `internal`), and every egress flow goes to peer kind `Service` port 5432 over private IPv6. **Egress reduction from switching "public" inter-service URLs = 0 MB — there are none to switch.** Only public hostnames are Railway-injected metadata (`RAILWAY_PUBLIC_DOMAIN`, `RAILWAY_STATIC_URL`) plus one user-made, unread var `RAILWAY_SERVICE_AVIBE_HINDSIGHT_URL`.
- DB target: **shared Postgres service, not the embedded volume** (startup log + pgvector alembic migrations; zero `pg0` lines even though the image ships pg0-embedded).
- Real repeat egress = **HF model fetch at container start**: `BAAI/bge-small-en-v1.5` (133.5 MB) + `cross-encoder/ms-marco-MiniLM-L-6-v2` (90.9 MB) ≈ **224 MB min**, measured 357.9 MB RX burst at the 2026-08-28 deploy. avibe-hindsight has **no volume**, so the HF cache dies on every new deployment. Proposals: bake into the image, or mount a volume at `/home/hindsight/.cache/huggingface` (**tier-2 → awaiting Ole**).
- PR Environments setting: `prDeploys = **false**`; only 1 environment (`production`, non-ephemeral); **0 ephemeral PR environments running**.
- Measured 30d (2026-08-22→09-21): egress **1,160.8 MB** (≈1,032 MB of it the 09-16/17 vault-ingestion→LLM burst), ingress 518.5 MB. Billed egress Sep 15–Oct 15 = **$0.0511** of a **$9.46** project total — memory is **$8.76 (93%)**, so egress optimisation is not where the money is.
- Incidental: detached orphaned volume `postgres-volume` (134 MB, still charged); `railway status` shows avibe-hindsight as `● Completed` while logs stream live (cosmetic).
- No changes made; no code touched, no service deleted.

## memory-curator · nightly repo→vault sync + auto-push (22:30 cron)

- `repo_sync.py --dry-run`: **zero drift** — updated=0, new=0, identical=43 across all 4 mirrored repos (LexFlow-landing 2 · LexFlow-Chatbot 12 · aLEXy 24 · avibe-hindsight 5). No `--live` run needed; mirrors already current.
- `git_autocommit.sh`: pushed `55188e5..646f4e4` → `Ole00007/obsidian-vault` main. **Verified** `git log -1 origin/main` = `646f4e4` and `main...origin/main` = 0/0 (script's "pushed" string is not code-checked — always verified).
- Commit carried 8 files / 229 insertions: these three notes (project status, Hindsight read-auth runbook, egress audit), `_Meta/cron-jobs/self-improvement-log.md`, and 4 org-chart PNGs.

## Links
- Parent: [[05-Daily-INDEX]]
- Related: [[LexFlow-Project-Status]]
- Related: [[LexFlow-INDEX]]
- Related: [[Hindsight-Read-Auth-Runbook]]
- Related: [[2026-09-20]]
