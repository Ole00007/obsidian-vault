# 2026-09-01 — Daily Note

## operator-installer · LexFlow CRM session (morning)

### Deployed & verified (prod `web-production-031a6`, `db: ok`)
- **Romanelli entry flow fixed:** LP buttons («🔐 Area riservata» nav + «LexFlow CRM» FAB) → `/login?ws=romanelli-studio&back=<site>` (AREA RISERVATA). Login = `preview@romanelli.test` / `Romanelli0826`. Worker redeployed (Version `c15cde68`); `Cache-Control: no-cache` on HTML so stale buttons stop appearing (commit `4a41399`).
- **Romanelli repo cleaned + pushed:** removed stale nested `lexflow-crm/` (secret-bearing, broke deploys), history rewritten to single clean commit `972901e`, pushed `Ole00007/Romanelli-studio` (backup `/tmp/romanelli_lexflow_crm_bak`).
- **Login UX:** after login → `/kanban` (not /dashboard); dashboard eyebrow → "Your Secure Workspace on LexFlow"; kanban back-to-site → correct full workers.dev URL (commit `86659ff`).
- **Per-workspace favicon + Secure Workspace banner on ALL pages** (client-side enhancer in base.html via /api/workspace; kanban got favicon + fixed stale "aLexy" title) — commits `809b4d0`, `7e89cbe`.
- **Calendar grid (Google-style Day/Week/Month)** merged + deployed (commit `cc29045`).
- **SECURITY (real leak fixed, live-verified):** `/admin` showed ALL clients' cases anonymously → now auth-required + scoped; `/admin/matter` + `/status` scoped for staff (foreign ws → 404); anonymous `/admin` → 302→/login (commits `a639362` + `cec79dc`).
- **Bugs fixed:** `/submit` 500 (ws fallback to non-existent ws#1 → resolves auth→?ws→lexflow→first active); `/api/tasks` 500 (duedate year 92026 → boot-heal + strict parse + safe to_dict); "toast is not defined" on /tasks (global toast() in base.html). Verified live: /api/tasks 200.
- **Uploads persistence:** Railway volume `web-uploads` attached to web at `/app/uploads` (verified with real file upload).

### Architecture / isolation (audited, answers given to Ole)
- Single Flask app + single Postgres, row-level isolation via `workspace_id`. No per-client DB/frontend; React NOT used (vanilla JS).
- Isolation verified live: superadmin sees 35 cases (all ws), Romanelli sees 3 (ws10 only), Pagliano sees 3 (ws9 only).
- Stale-JWT-in-browser explains "entered LexFlow → saw Romanelli": browser localStorage holds last token; LP buttons pointing to /kanban auto-login with it. Fix pattern (→ /login?ws=) applied to Romanelli; Pagliano pending.

### Pending / held
- RLS (flask-rls): backend-dev finished on feat/rls, NOT tested/merged — joint staging test pending Ole.
- Pagliano: LP button repoint → /login?ws=pagliano + "Pagliano Secure Workspace" branding + favicon (held until architecture decision; Ole approved full name since no leak).
- Google Calendar sync + self-service password + 2FA plan (deferred — sensitive work, Ole does not do at night).
- Morning Telegram report (07:30) + nightly watchdog confirmed delivering (chat 1372207688).

## Links
[[05-Daily]]
[[LexFlow-Workspace-2026-08-31-Inventory]]
[[LexFlow-MultiTenant-Test-Setup]]
