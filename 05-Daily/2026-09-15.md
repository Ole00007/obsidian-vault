# 2026-09-15

## LexFlow /api/public/intake contract review — answered frontend-developer's card (operator-installer)
- Ran kanban card `t_4c31dac3`: verified the site's new `.well-known/openapi.yaml` contract against the real CRM (`LEXFLOW Production/lexflow-crm`, HEAD `fddbb7f`) and live prod `web-production-031a6.up.railway.app`.
- Verdict: **contract matches**, implement as written. 4 deltas (`source` column is 20 chars; response id type string vs int; Contact-only vs Contact+Case; stricter per-route rate limit). 4 decisions for Ole (D1 contact-only, D2 tags for practice_area/lang/consent_marketing, D3 workspace routing, D4 aLEXy rename deploy).
- Two frontend claims corrected with evidence: anonymous `GET /api/contacts` returns **200 with `[]`** (not a data leak — `workspace.py` returns `[]` for anonymous by design), and **CORS already allows both Netlify origins** in prod (not localhost-only).
- Blocked cards `t_d6065167`, `t_85d62411`, `t_8c6d4642`: all six crashed runs were "worker exited rc=0 without completing" from the Nous auth failure. operator-installer now runs on openrouter `deepseek/deepseek-v4.1-flash` and this card completed — blocker empirically resolved. `t_8c6d4642` (site claims audit) is a separate task, still unstarted.
- Logged: [[01-Projects/LexFlow/LexFlow-Public-Intake-Contract-2026-09-15|Public intake contract note]]

## LexFlow web-site Package 1 review — answered frontend-developer's card t_35c677e0 (operator-installer)
- Reviewed the Elisa copy + "functions available now" proposal at Web-Site HEAD `88f6cc9`; re-probed prod. Review only, no site writes.
- **Corrections:** contacts/cases return `200 []` (empty, not readable); `chatbotUrl` IS set in site-config (path 404 + unused) and the WhatsApp number is still a TODO; `/submit` (302) and the Netlify CORS allow were missing; the "calendar/cloud storage/notifications not live" line needs a *site-path* qualifier or it repeats the understatement bug.
- **Chatbot copy premise false at HEAD:** all three COPY dicts complete and identical (44 top-level keys each); only `c.urgencyLabel` (L263) + `c.needDescribe` (L276) are referenced but missing. Fallback must be a merge (`Object.assign`), since `t()` returns the whole dict.
- **Elisa has no chat logic:** `CHAT_PATH` is dead code; only `fetch` is POST to `intakeEndpoint`. Elisa sends `source='lexflow-website-elisa'` (21 chars, overflows `String(20)`).
- Card status: `t_d6065167`/`t_85d62411` superseded; `t_8c6d4642` still unstarted; `t_7bf4de1f` held pending Ole D1–D4.

## Links
- Project: [[01-Projects/LexFlow/LexFlow-Public-Intake-Contract-2026-09-15|LexFlow Public Intake Contract]]
- Related: [[LexFlow-INDEX]]
- Logged: [[01-Projects/LexFlow/LexFlow-Web-Site-Enrichment-Elisa-2026-09-13|LexFlow Web-Site Enrichment]]

## LexFlow web-site catch-up report — answered operator-installer's card t_da0d10f9 (frontend-developer)
- Report only, no site edits. Confirmed: HEAD `88f6cc9`, clean `main`, single worktree, no remote (local-only). Work continues directly on `main`.
- Corrected operator's sitemap count: coverage is **18 EN + 15 IT + 15 RU** (not 16 EN) — extra EN pages are privacy/terms (cookie-policy is a 3rd EN-only); 48 loc / 180 hreflang / 48 lastmod, all at placeholder `lexflow.example.com`; nothing stale after `b9b406a`/`88f6cc9`.
- Docs triage: `llms.txt` (authoritative product facts), `verification-plan.md`, `claims-register.csv` accurate; `README.md` stale (still "Alessia", intake "posts to CRM contacts API" is false — `intakeEndpoint` null, falls back to WhatsApp; hook.example.com gone), `i18n-audit-hardcoded-strings.csv` stale, `lexflow-local-dir.md` wrong (parent is `LEXFLOW Production` not `Proc[ess?]`).
- Confirmed miss: `llms.txt` line 18 ("calendar/cloud/notifications NOT shipping") contradicts `faq_a15` fixed by `0291704`; b9b406a refreshed llms.txt but kept the stale line. First fix next = align llms.txt to "available today".
- Blockers (9) listed with owners; 27 webhook URLs + intakeEndpoint null; WhatsApp digits unconfirmed; IT/RU article bodies + D1–D4 pending Ole.

## LexFlow Web-Site agent-facing surface — logged 2026-09-16 (memory-curator, card `t_c421f430`)
- Captured the site's agent surface in the vault: `.well-known/ai-plugin.json` (guardrails: no legal advice; ISO 27001/27701 in progress not held; SOC 2 is a framework), `.well-known/openapi.yaml` (`POST /api/public/intake`, contract-defined but returns 404 live), `assets/webmcp.js` (6 WebMCP tools via `navigator.modelContext.registerTool`), refreshed `llms.txt` (+"Machine-readable and agent surfaces"), the Organization+WebSite+SoftwareApplication JSON-LD upgrade, and the 48-URL sitemap.
- Durable facts: agent surface committed `b9b406a` (repo HEAD now `88f6cc9`); no git remote and no deploy config; 74 files still hold `lexflow.example.com`; 33 nulls in `site-config.js`; localised article bodies must be registered in `templates/article_content.py` or they silently stay English.
- Full detail: [[LexFlow-Web-Site-Agent-Surface-2026-09-15]]
