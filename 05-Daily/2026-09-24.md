# 2026-09-24

## memory-curator · БЕЗОПАСНОСТЬ: публичный vault + утечка ключа (запрос Ole)

- **P0 подтверждён и уточнён.** `Ole00007/obsidian-vault` — **PUBLIC** (создан 2026-08-28, 1308 отслеживаемых файлов, ~67 МБ, forks=0). Внутри публично: клиентские папки (AVibe-CRM 291, Alena-Krot 52, Studio-Romanelli 25, Carrozzeria-2DI 17, aLEXy 26, sites 53), 28 дневных заметок, `_Trash/`, `_Inbox/_Conflicts/`. **11 отслеживаемых файлов содержат клиентские тест-креды и телефон клиента.**
- 🔴 **НАЙДЕН ЖИВОЙ СЕКРЕТ:** Google Gemini API-ключ (`AIzaSyDR-i…`, значение в чат не выводится) захардкожен в `01-Projects/AVibe-CRM/LexFlow/test_gemini.py` и `01-Projects/AVibe-CRM/aLEXy/test_gemini.py`; закоммичен 2026-08-27 первым коммитом vault, присутствует в `origin/main` → **публично ~26 дней, ключ НЕ отозван**. Требуется ротация у провайдера — единственное реальное исправление.
- **Корневая причина канала утечки:** активный `downloads_stager.py` (профиль memory-curator — именно его запускает cron `05a20eff1b3e`) **регрессировал** и потерял блок `EXCLUDE_PATTERNS`, который сохранился в бэкапе `_Meta/cron-jobs/downloads_stager.py`. Следствие: `client_secret_…googleusercontent.com.json` из `~/Downloads` был кандидатом на стейджинг → vault авто-коммитится и пушится → публикация OAuth-секрета.
- ✅ **ИСПРАВЛЕНО (код, не выполнено развёртывание):**
  - stager: восстановлен и расширен credential-guard (`is_sensitive()` + `EXCLUDE_PATTERNS`: client_secret, google_token, oauth, service_account, id_rsa/ed25519, .p12/.pfx/.keystore/.sqlite/.db, wallet/recovery). Проверено dry-run: `excluded_sensitive: 2` (OAuth-секрет теперь блокируется), `staged` 4 → 2.
  - stager: identity-проверка переведена на **md5 по всему vault** (а не только по `_Inbox`) — закрывает 100% ложных карантинов недели. Проверено: `skipped_identical_vault: 58` (было 57 + исключённый секрет).
  - `.gitignore` vault: добавлен OAuth/secret-блок (client_secret*,google_token.json, *_secret*.json, service_account*, .env, id_rsa*, .keystore, .ovpn). Проверено `git check-ignore -v` = ловит.
  - Бэкап `_Meta/cron-jobs/downloads_stager.py` синхронизирован (md5 совпадает: `4386c5e9edd0e1eaf0aade795b99d87b`).
  - Создан отсутствовавший хаб `_Inbox/_Inbox-INDEX.md` → закрывает 57 битых `[[_Inbox-INDEX]]` (44% всех битых ссылок).
- ⏳ **НЕ сделано (нужно решение Ole):** отзыв ключа Gemini; перевод репо в private; очистка истории (filter-repo/BFG + force-push) или пересоздание репо; удаление 2 файлов с ключом из рабочего дерева.
- ⚠️ **Владелец watchdog-задачи — другой профиль.** `lexflow-nightly-watchdog` (job `20b25bd341d5`) живёт в профиле **`smoothy_op_dir`**, не в memory-curator → по правилу изоляции профилей не изменяю. Причина падения: в джобе путь `…/LEGAL/lexflow-crm`, а реальный репо — `…/LEGAL/LEXFLOW Production/lexflow-crm` (пропущен каталог «LEXFLOW Production»). Ту же ошибку пути несут ещё 2 джобы в `smoothy_op_dir`. Передать владельцу.
- ⚠️ **Коллизия хабов:** два файла `LexFlow-INDEX.md` (`01-Projects/LexFlow/` и `01-Projects/AVibe-CRM/LexFlow/`) → 59 ссылок `[[LexFlow-INDEX]]` неоднозначны. Переименование затронет клиентскую папку (291 файл) — предложено, не выполнено без подтверждения.
- Создан черновик hard rule для всего ростера: [[2026-09-24-Hard-Rule-DRAFT-secret-egress-prevention]] (7 пунктов §S1–§S7).
- ⚠️ Cron `p0-vault-public-decision-brief` (532afe57bf73) создан на 10:04 с доставкой в Telegram, но **gateway не запущен** → не сработает, пока gateway не стартует.

## memory-curator · Part F nightly self-improvement log (22:00 cron)
- Logged today: 3 quarantines (`City-Address-Phone-Email-Hours-Website.csv`, 2 × `WhatsApp Ptt 2026-09-22` .ogg) — **all 3 byte-identical false positives**; `_Conflicts` flat at 52 (49 tracked + 3 untracked); 18:00 stager green and its restored credential-guard blocked `app.db` + the OAuth `client_secret_….json`; the 09-23 whole-vault-md5 fix is live in `downloads_stager.py` but **not** in the 05:13 `obsidian-staging.sh`. Entry: [[self-improvement-log]].

## memory-curator · nightly repo→vault sync + auto-push (22:30 cron)
- `repo_sync.py --dry-run`: **0 UPD / 0 NEW**, 43 docs identical across 4 mirrors (LexFlow-landing 2, LexFlow-Chatbot 12, aLEXy 24, avibe-hindsight 5) → `--live` not needed, no drift.
- `git_autocommit.sh`: committed + pushed `668637a..f540e27`. **Verified** `main == origin/main` at f540e27, ahead/behind `0/0`, working tree clean.
- 🔴 **NEW EGRESS FINDING (escalates today's P0).** That push published to the *still-public* repo, first time on `origin/main`: `_Inbox/rasing_dossier_v4.zip` (personal dossier), a client contact CSV under `_Trash/` (client PII), and 2 × WhatsApp `.ogg`. The `.gitignore` hardening from earlier today does not cover `_Inbox/*.zip`, `_Trash/`, or media. The Gemini key files `test_gemini.py` (LexFlow + aLEXy) were re-touched by this commit — those keys remain public and unrevoked.
- ⚠️ Consequence: **every auto-push is currently a public egress channel.** The nightly cadence makes the P0 worse, not neutral. Fix is one of: repo → private, or extend the guard to `_Trash/`, `_Inbox/*.zip`, media, plus history rewrite. Needs Ole's decision (already queued in today's P0 brief).
- Job outcome: success (sync + push + log). No secrets staged; script guard passed.

## Links
- Parent: [[05-Daily-INDEX]]
- Related: [[2026-09-23]]
- Related: [[2026-09-24-Hard-Rule-DRAFT-secret-egress-prevention]]
- Related: [[2026-09-23-AVIBE-Ecosystem-Master-Table]]
