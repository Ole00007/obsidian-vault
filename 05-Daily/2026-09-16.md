# 2026-09-16

## operator-installer
- Fixed `memory-curator` dead-model chain (all `:free` models 404) → `deepseek/deepseek-v4.1-flash` + live fallback; verified live. Its weekly vault cron jobs had been silently failing.
- Delegated the broken vault→Hindsight sync fix to `memory-curator` (background run; ticket covers diagnose → token → fix → prove → durable nightly job).
- Wrote agent-roster consolidation proposal for the 3 orphan profiles (`chatbot_builder`, `email-digest-agent`, `tester`) → `_Inbox/Agent-Roster-Consolidation-Proposal-2026-09-16.md`; awaiting Ole.
- CRM focus: fixed aLEXy→LexFlow naming in `templates/index.html` + `templates/status.html` (deployed app still self-identified as "aLEXy – Legal Intake & Status Suite"). Local commit `a65a723` on `lexflow_hermes_v1`; verified by rendering locally on port 5057 (title + h1 now "LexFlow – Legal Intake & Status Suite"). NOT deployed — awaiting Ole's go.

## memory-curator
- Repaired the broken vault→Hindsight sync (`com.avibe.obsidian-sync`). Root cause: the reconcile sent no API credential (401) and the CLI aborts the whole run on a vault template carrying the placeholder `date: {{date}}` (422) — and `set -e` + `$(...)` swallowed both errors, leaving the 58-byte log. Fixed the token (resolved at runtime from the profile hindsight config, no secret in the script), added an exit-code check + rolling status log, and excluded that one placeholder skeleton by exact path.
- Proof: main sync index `lastSyncAt` 2026-08-26 → 2026-09-16T12:57Z (270 → 379 entries), run reported `+185 added, ~174 updated` exit 0, `launchctl start` run exit 0, gap notes (`LexFlow-Public-Intake-Contract-2026-09-15.md`, `05-Daily/2026-09-13/15.md`) indexed. Report + open items: [[Hindsight-Vault-Sync-Fix-2026-09-16]].

## Links
- Parent: [[05-Daily-INDEX]]
- Related: [[AVibe-Agency-INDEX]]
- Related: [[Hindsight-Vault-Sync-Fix-2026-09-16]]