# 2026-09-16

## LexFlow RLS Hardening Audit (t_4637a500)

- Conducted comprehensive audit of LexFlow Row-Level Security status
- Confirmed RLS IS BUILT on `feat/rls` branch (migration + 18 tests) but NOT MERGED
- Mapped all 8 tables covered by app-level `workspace_filter()`
- Identified 2 gap tables (`contact_relationship`, `firm_team_member`) not covered by either system
- Documented 5 Alembic compatibility risks (custom methods, downgrade symmetry, NOBYPASSRLS prerequisite, ExpressionPolicy edge case, flask-rls dependency)
- Drafted full delegation ticket with table count (11 RLS tables + 2 gap tables = 13 total scope)
- Produced: findings_and_delegation_ticket.md + vault note → awaiting Ole's filing decision (Linear / GitHub / direct handoff)

## Vault logging gaps closed + LexFlow ecosystem inventory v3 (memory-curator, card `t_a261ac01`)
- Closed the three outstanding vault-logging cards: `t_c421f430` (agent-facing surface → [[LexFlow-Web-Site-Agent-Surface-2026-09-15]]), `t_6c2c8c07` (2026-09-13 recap → cross-check section appended to [[LexFlow-Web-Site-Enrichment-Elisa-2026-09-13]]), `t_76668155` (batch-2 recap → [[LexFlow-Web-Site-Session-Recap-Batch2-2026-09-13]]). New notes land in `_Inbox/` per AGENT_RULES; hub updated.
- **Ecosystem index v3** written to `LEXFLOW Production/Lexflow Ecosystem & Saas Factory/LexFlow_Ecosystem_Index_v3_2026-09-16.md` + `.xlsx` (6 sheets); v2 md marked superseded. Vault mirror in `_Inbox/`.
- **Live endpoints re-probed:** CRM `web-production-031a6` 200 (serves "aLEXy – Legal Intake & Status Suite"); `/login?ws=lexflow` 200; `/admin/panel` 200; `/kanban` 200; `POST /api/contacts` 401; `/api/public/intake` 404; `/api/health` 404; chatbot `/health` 200; hindsight `/health` 200.
- **URL corrections:** the live legacy LP is `poetic-kleicha-28d058.netlify.app` (200) — old docs dropped the suffix; `lexflow-mvp-production` root is 404; `lexflow.netlify.app` exists but is password-protected (401, ownership unverified); `lexflow.pages.dev` belongs to a South African company, NOT us.
- **Answer to "where does the site deploy": nowhere yet** — the Web-Site repo has no git remote and no deploy config. 10-item gaps/blockers table (host/domain, intake endpoint, llms.txt contradiction, RLS merge, tokens) in the v3 file.

## LexFlow CRM Cross-Feature Consistency Audit (t_4f1a9c3c)

- Tested prod API (https://web-production-031a6.up.railway.app) — 4 issues found
- File upload on /tasks: 404 on prod (no attachment endpoint deployed)
- Intake → Calendar: FAIL (no Event creation from intake)
- Tasks → Calendar: FAIL (eventid FK exists but no API route links them)
- Workspace filtering: FAIL (no ws-based filtering in any API route)
- Full report + fix plan written to report.md → task completed, no deploy without Ole's go

## LexFlow LP → CRM CTA wiring (t_198870bd) — Сделано ЛОКАЛЬНО

- Landing repo found moved: `~/LexFlow-landing` → `~/Projects/LEGAL_backup/LexFlow-landing` (brief path stale)
- All 7 LP CTAs verified on the workspace deep link `/login?ws=lexflow&back=<landing>`; 0 bare-root, 0 `/admin`
- Fixed the dead `href="#"` footer privacy note; merged `origin/main` into `main` (divergence ahead 4/behind 1 → ahead 5/behind 0); repointed the hold branch (5× root + 2× `/admin`)
- Live LP = `poetic-kleicha-28d058.netlify.app` (sha256 == `origin/main:index.html`); `poetic-kleicha-c96710` / `poetic-kleicha` / `lexflow-landing` all 404
- Local preview http://127.0.0.1:8899/index.html — no deploy, no push
- Note: [[LexFlow-LP-CRM-CTA-Wiring-2026-09-16]]

## LexFlow legal publishing blockers — pencilled, reminder to Ole (t_051169c7)

- Registry note written (no page edited): [[LexFlow-Legal-Publishing-Blockers]]; pencilled into the CRM plan note §7 ([[LexFlow-2026-09-16-Stack-Decision-and-Task-Board]]) + hub updated.
- Verified read-only on the Web-Site repo (HEAD 88f6cc9): `privacy.html:75` still shows "Studio Legale X" **unflagged**; `cookie-policy.html:47,48,94` hold `[LEGAL COMPANY NAME]` / `[REGISTERED ADDRESS]` / `[NUMBER]` / `[privacy@lexflow.example]` / `[PEC]` with "Review before launch" at :49.
- **New finding beyond the card:** the 9 article pages (× EN/IT/RU) carry `demo@lexflow.com` only, while FAQ/pricing/practice-areas/how-it-works (lines 270–271 / 179–180 / 293–294 / 153–154) carry both + `llms.txt:60` — three contact patterns, not two.
- **Gate = P.IVA.** Entity is still "Studio Legale X", so no VAT number exists → `privacy.html`/`cookie-policy.html` are unpublishable. One combined reminder to Ole; card `t_051169c7` blocked as needs_input.
- Do not edit these pages until Ole supplies: legal entity name + form, registered office, VAT/P.IVA, PEC (or postal decision), privacy contact, and the `info@` vs `demo@` decision.

## LexFlow LP → CRM CTA wiring (t_198870bd) — review round 1: CHANGES REQUESTED

- Independent review (run 28, artifact lens) verified the core claim: 7 CTAs on `/login?ws=lexflow&back=<landing>` in source **and** rendered DOM; `ab54f` gone tree-wide; live sha256 `2cdf82a9…` == `origin/main:index.html`; `main` ahead 5/behind 0; `ws=lexflow` is a real workspace (login page renders tenant "LexFlow Default" vs "LexFlow" for other/empty `ws`).
- **Blocked on 3 correctable defects:** (1) handed-over preview `http://127.0.0.1:8899/index.html` served an empty reply (hung PID, curl `000` 3/3) — Ole could not review it; (2) `lexflow-landing-ready-for-deployment.html` is publicly live (200) with 8 refs to the dead `lexflow-mvp-production` host and 0 to `031a6`; (3) `index.html:1447` `CHATBOT_URL` POSTs to the CRM bare root → 405, widget can never work — real endpoint is `POST /api/intake/lexflow` (needs fullname+email+gdpr).
- Corrected a false citation carried in the report: no `LEXFLOW_SITE_URL` exists in the CRM repo; the binding is the workspace seed row in `lexflow-crm/migrations/versions/3a6b84c5d9f2_add_multitenant_workspaces.py:35`.
- No deploy, no push; task returned to frontend-developer-lovable_react. Note: [[LexFlow-LP-CRM-CTA-Wiring-2026-09-16]]

- **Roster change (Ole 2026-09-16): `librarian` merged into `memory-curator` and retired.** Its roadmap card `t_62db7c26` crashed twice under librarian (worker exited rc=0 without `kanban_complete` — model/protocol failure), so it was reclaimed and reassigned to memory-curator (now running). Role text + knowledge-management duties folded into memory-curator's `SOUL.md`; a stale file-taxonomy + SOP register folded into its `MEMORY.md`. Pre-delete archive: `~/.hermes/profile-exports/librarian-20260916-161325.tar.gz` (restorable). Roster is now 22 profiles.
- **Hard rule set: Repo Root Rule §8** added to `~/Obsidian/_Meta/AGENT_RULES.md` and injected into every profile's `SOUL.md`: all repos at `/Users/olesiarasing/projects/<repo>` (lowercase), `~/Desktop/projects` frozen read-only, no `~/Projects` casing. Staged migration (LEGAL first) runs after the frontend deploy, with Ole's go per batch.
- Frontend card `t_198870bd` still running — website CTAs → `https://web-production-031a6.up.railway.app/login?ws=lexflow` (pointed at the CRM for project completion; repoint pass follows later).
  **Correction 16:20 (Ole, mid-run):** the shipping target is now the **CRM entry door** `https://web-production-031a6.up.railway.app/admin/panel` (superadmin door) in all 7 CTAs; the per-tenant `/login?ws=lexflow` deep link is deferred to the later repoint pass.

## Kanban: 3 LexFlow vault-logging cards closed (t_f3c6dee2)

- Verified the three artifacts on disk, then closed `t_c421f430`, `t_6c2c8c07`, `t_76668155` (all `done`, results name `t_f3c6dee2` as closer). Vault logging was already complete — only the board rows were stale.
- Learned the hard way: **no agent worker can close a sibling card.** `tools/kanban_tools.py` scopes a worker to `$HERMES_KANBAN_TASK`, and `kanban_db._assert_not_delegated_child_mutation` refuses shelled-out `hermes kanban complete` from a worker's own shell. Supported channels are `kanban_comment` / `kanban_create`; closure needs a human session. See [[Kanban-Cross-Card-Closure-Guards-2026-09-16]].
- Same `rc=0` without `kanban_complete`/`kanban_block` crash that hit `t_62db7c26` put all three in `blocked`.
- Ole to resolve: `lexflow.netlify.app` (401 password-protected, ownership unverified); new Web-Site repo deploys nowhere (no remote, no deploy config).

## Hermes 14-day installation roadmap (memory-curator, card `t_62db7c26`)

- Draft written: [[Hermes-14-Day-Installation-Roadmap-2026-09-16]] (`_Inbox/`) — proposal only; **no profile created, merged or deleted, no connector connected**.
- Covers: 22-profile roster → brief's 6 roles, 14-day phased plan (stabilise → identity/memory/skills → connectors/routing → E2E tests → autonomy), connector checklist (add/fix vs quarantine the keyless MCP servers), memory structure (`SOUL.md` / `MEMORY.md`+`USER.md` / vault / Hindsight — *not* new profile.md/memory.md/soul.md files), skill list (6 new, 4 mapped), cheap/standard/premium routing policy, 3 E2E test plans, budget guardrails, 10-item human-confirmation list.
- **Evidence found while drafting (all read-only):** Nous Portal auth is stale (`No access token found for Nous Portal login` — breaks the "Nous primary" half of stack 1.4); 2 `operator-installer` crons fail on `ImportError: _FULL_ARGS_LOG_BOUND`; cron→Telegram delivery fails with `httpx.ConnectError` (DNS); 6 `memory-curator` crons last errored on the dead `qwen/qwen3-4b:free`; `GITHUB_PERSONAL_ACCESS_TOKEN`/`COMPOSIO_API_KEY` set in no profile; 3 profiles (`chatbot_builder`, `email-digest-agent`, `tester`) still cannot boot.
- Proposed final home (needs Ole's approval): `03-Resources/Hermes-Setup-and-MCP/` — beside [[hermes_setup_brief]] and [[Agent-Profiles]]; **not** `01-Projects/LexFlow/` (covers SEO/CRM/Telegram too).
- **r2 revision 2026-09-16 (post-review, card `t_62db7c26` run 39):** §0/§4/§6 counts re-verified against the live estate — `OPENROUTER_API_KEY` is on **19/22** profiles (not 20); the provider split is `openrouter` **10** / `google` **8** / `nous` **3** / `tester` none (not 9/7/3 — the field is nested as `model.provider` at line 3 of each `config.yaml`, so a body-level `provider:` grep finds nothing); **3** profiles cannot boot (not 2). New evidence row: the dead `:free` IDs sit on two *primary* defaults (`lexflow_dev_head_admin` = `minimax/minimax-m3:free`, `personal-assistant` = `tencent/hy3:free`).

## LexFlow LP CTA wiring — round 2 (t_198870bd, frontend-developer)

- **CTA target changed mid-run by Ole:** all 7 CRM-facing links (navbar, hero badge, hero CTA, 2× closing CTA, footer App/Admin) + the mock caption in both HTML pages now open the **CRM entry door** `https://web-production-031a6.up.railway.app/admin/panel`; the per-tenant `/login?ws=lexflow&back=…` link stays in history for the later client-facing pass. Commits `4b5d61d`, `47ed616`, `293da63`, `55d5488` on `main` (local only, `origin/main` untouched, nothing deployed).
- **Сделано ЛОКАЛЬНО** — preview http://127.0.0.1:8899/index.html. Round-1 review defects closed: the hanging preview server replaced with a threaded `serve_preview.py` (the stock `python3 -m http.server` keeps LISTEN but answers nothing after a Chrome sweep — twice), the publicly served duplicate page's 7 dead legacy refs repointed, the bare-root CRM POST removed (page now makes 0 network calls), the false `LEXFLOW_SITE_URL` citation corrected.
- Follow-up card **`t_62cbf086`** created (widget → `POST /api/intake/lexflow` needs Ole's sign-off on new name/email/GDPR fields + IT/EN consent copy). Detail: [[LexFlow-LP-CRM-CTA-Wiring-2026-09-16]].
- **Dispatcher lesson:** the review run stayed alive past its own `changes_requested` verdict and committed the rework itself (`ee4836f` → `293da63`) while the rework run verified the same files — two workers on one card/repo.

## Operator-installer cross-check: LP deliverable verified, widget decision needed (t_62cbf086 / t_198870bd)

- Independently re-verified the handed-over LP build (not from the report): served bytes `sha256 46c22a40…` == `HEAD:index.html` (`main` `55d5488`), preview `http://127.0.0.1:8899/index.html` **200** (PID 15315); 7 CRM hrefs in served bytes, all `/admin/panel`; 0 legacy hosts, 0 dead `#`; **0 network calls** in the widget.
- Probed the real intake endpoint read-only: `POST /api/intake/lexflow` `{}` → `400 Name and email are required` (no record written); `OPTIONS` preflight from the Netlify origin → 200 with the origin echoed. Handler `crm/routes/views.py:268-337` re-read.
- Two WARNs found beyond the handoff: `gdpr_consent` is stored but **not enforced** (submit with `gdpr=false` still creates the record), and the public route relies on the global `50/hour per IP` default limit over `memory://` storage (per-worker counters) rather than an explicit route limit.
- Opened card `t_62cbf086` (assignee operator-installer) is dispatched and asking for Ole's sign-off: new `fullname`/`email`/consent fields in the Alessia widget = a visual change, and the consent line would reference an "informativa privacy" that **does not exist** on this landing (entity still "Studio Legale X", no P.IVA). Decision package + IT/EN copy draft appended to [[LexFlow-LP-CRM-CTA-Wiring-2026-09-16]].
- No deploy, no push, no record written by this session.

## t_62cbf086 recon: widget→intake wiring — blocked on Ole's sign-off (copy + test-submit)

- Re-verified the endpoint live before touching anything: `OPTIONS /api/intake/lexflow` 200 + ACAO echoed from the Netlify origin and from `localhost:3000/5000/5001`; **`127.0.0.1:8899`, `127.0.0.1:5000` return no ACAO** → the browser blocks the POST, so the functional preview must be served on `http://localhost:5000` (not the `:8899` port used by the parent card). `POST {}` → 400 `Name and email are required` (validation path, writes nothing); unknown slug → 404 `Invalid workspace`.
- `lexflow-landing-ready-for-deployment.html` contains **0** `alessia` occurrences → the change is a single-file edit (`index.html`).
- Posted the full decision package to the card: proposed field set (fullname, email, phone optional, GDPR checkbox, submit), IT/EN consent + success/error copy, branch plan `feat/alessia-widget-intake`, headless fetch-interception verification, and the two gated items (copy approval; permission for one real submit that creates a Contact+Case and sends 2 emails).
- Card moved to **blocked / needs_input**. No file edited, no record written, no push, no deploy.

## Operator-installer: Phase-0 plumbing — stale gateway found as the single root cause

- Ran the roadmap's Phase 0 (stabilise plumbing) against live state. **One finding replaced four reported failures:** the estate's cron ticker runs in-process inside the `operator-installer` gateway (`PID 12005`, started Sun 2026-09-13 14:01). That 3-day-old process (a) holds pre-pull module objects, so agent-mode cron jobs die on `ImportError: _FULL_ARGS_LOG_BOUND` even though `agent/message_sanitization.py:132` defines it after the 09-15 11:42 pull, and (b) has a rotten resolver — every outbound call returns `000` / `nodename nor servname provided`, which is what made `lexflow-try-demo-daily-check` "fail" all six probes and what breaks Telegram delivery.
- Verified in fresh processes: `from agent.message_sanitization import _FULL_ARGS_LOG_BOUND` → OK; `dig`/`curl api.telegram.org` → 302; prod `health=200`, `login?ws=lexflow=200`; `bash scripts/lexflow-try-demo-check.sh` → exit 0, no FAILURES. So none of it is a config error — **fix = restart that one gateway**, sequenced between cards (it is the parent of in-flight workers).
- **Fixed and smoke-tested two dead profile defaults:** `lexflow_dev_head_admin` `minimax/minimax-m3:free` → `deepseek/deepseek-v4.1-flash`; `personal-assistant` `tencent/hy3:free` (provider `nous`) → `google/gemini-3.5-flash` on `openrouter`. Both old IDs reproduce live HTTP 404; both profiles now boot `OK`. `personal-assistant` was doubly broken — its provider's auth is stale.
- **Pending Ole:** Nous Portal re-auth (interactive) — blocks the `nous` provider, stack decision 1.4's primary half, and `agent-roster-friday-kpi`; gateway-restart timing; `GITHUB_PERSONAL_ACCESS_TOKEN` / `COMPOSIO_API_KEY` exist in no profile.
- Six Phase-1 skills delegated to three parallel subagents (`deployment-checklist`, `cost-guardrails`, `feature-spec-writer`, `legal-tech-domain-notes`, `crm-outreach-sequencing`, `telegram-short-reply`) — writing into `~/.hermes/profiles/operator-installer/skills/`.
- Note: [[Hermes-Gateway-Staleness-Root-Cause-2026-09-16]]

## Memory-curator: review r2 of the 14-day installation roadmap (card t_62db7c26)

- Reviewed r2 of [[Hermes-14-Day-Installation-Roadmap-2026-09-16]] under the execution lens. Reproduced the three prior corrections (19/22 keys; provider rewrite — the implementer's withdrawal of the earlier provider finding was correct, `model.provider` is nested at line 3; 3 cannot-boot profiles).
- Requested changes on four correctable defects: §4.1 Notion count says 6 but lists 5 (missing `operator-installer`); §6.1 "default on 8 profiles (`google/gemini-3.5-flash`)" conflates provider count with model count (true model count 10, incl. `agency-growth` + `seo-aeo-expert` on openrouter); §0/§2.1 estate facts made stale mid-review by the 17:15 model fix; and `chatbot_builder` does have MEMORY.md/USER.md + a configured model, so "no keys, no model, no memories" is inaccurate.
- Independently confirmed operator-installer's 17:15 remediation of the two dead profile defaults (`lexflow_dev_head_admin`, `personal-assistant`) — both now boot. No implementation files were edited by this reviewer.

## Memory-curator: r3 corrections to the 14-day installation roadmap (card t_62db7c26)

- Applied the round-2 review's four requested corrections to [[Hermes-14-Day-Installation-Roadmap-2026-09-16]] and re-verified every §0/§2.1/§4/§6 figure against the live estate at 17:21–17:25 (note now `revision: r3`, 33.7 KB).
- Corrections landed: Notion keys 6 → **5** profiles (`operator-installer` has no Notion entry at all — the reviewer's list of 6 was wrong, not just the count); `google/gemini-3.5-flash` "default on 8 profiles" → **11** (8 google-provider + `agency-growth`, `seo-aeo-expert`, `personal-assistant` on openrouter — provider count ≠ model count); dead-ID row rewritten (both defaults repaired 17:15, `minimax/minimax-m3:free` still pinned on 3 aux routes of `frontend-developer-lovable_react`); `chatbot_builder` restated (has model + memories + 1 session; only the key is missing).
- Also caught in the same pass, beyond the review: memory-curator has **9** cron jobs (not 6), 8 erroring — 7 on the dead `qwen/qwen3-4b:free`, all on 2026-09-15 runs; provider split is now **openrouter 11 / google 8 / nous 2**; `lexflow_dev_head_admin` is not dormant (78 sessions, last 17:15).
- Added §12 "Re-verification kit" — the exact read-only commands that produced every number, plus the r3 correction log, so the next reviewer can reproduce rather than re-litigate. §0 rows now carry "verified as of" timestamps.
- No profile was created, merged, renamed or deleted; no connector connected.

## Memory-curator: review r3 of the 14-day installation roadmap (card t_62db7c26)

- Round 3 (contract lens): every original acceptance criterion passes (all 8 content areas, `_Inbox` draft + proposed home, frontmatter, 7 resolving wikilinks, proposal-only) and both prior `changes_requested` rounds verified as landed — incl. the implementer's correction that Notion keys are on **5** profiles (`operator-installer` has no Notion entry at all), which the r2 reviewer had wrong.
- Two correctable defects remain: §0/§7 skill-count range "59–117 per profile / ~2,200 total" is really **59–123** and **2,131** (max = `operator-installer`); §6.3's "rows not listed here were not verifiable from config" is false — `frontend-developer-lovable_react` has 11 named-ID aux routes and 4 are missing from the table (`triage_specifier`, `kanban_decomposer`, `profile_describer`, `curator`). Verdict: request changes (r4), scoped to those two rows.
- Reproduced independently: provider split `openrouter 11 / google 8 / nous 2`; `google/gemini-3.5-flash` default on 11; `memory-curator` cron 9 jobs / 8 erroring; `fallback_providers` 1/21 non-empty; Telegram `deliver: telegram:1372207688` failing with `httpx.ConnectError`. No implementation file was edited by this reviewer.

## Memory-curator: r4 corrections to the 14-day installation roadmap (card t_62db7c26)

- Applied the round-3 review's two findings to [[Hermes-14-Day-Installation-Roadmap-2026-09-16]] (now `revision: r4`, 35.9 KB, 465 lines): §0/§7 skill-count range **59–117 → 59–123** per profile and **~2,200 → 2,131** total (`find <profile> -name SKILL.md`; 117 only holds if the six nested `mlops/<group>/<name>/SKILL.md` dirs are excluded).
- §6.3 completed to **all 13** aux routes of `frontend-developer-lovable_react` — **11 named IDs** + 2 `auto` (`tts_audio_tags`, `monitor`) — adding `triage_specifier`, `kanban_decomposer`, `profile_describer` and `curator`, and removed the inaccurate "(Rows not listed here were not verifiable from config…)" clause. Both fixes re-verified on disk at 17:33; `## Links` unchanged (7 wikilinks).
- Resubmitted for review (r4). Hindsight retain still blocked by the OpenRouter free-tier daily cap (resets 2026-09-17 00:00 UTC); the vault note is the source. No profile was created, merged, renamed or deleted; no connector connected.

## Memory-curator (cron): downloads_stager.py failed — TCC denial on ~/Downloads

- `downloads_stager.py` crashed with `PermissionError: [Errno 1] Operation not permitted: '/Users/olesiarasing/Downloads'` at `os.listdir(DOWNLOADS)` (line 73). No manifest written for 2026-09-16; last success was 2026-09-15 11:42.
- Cause: the agent's interpreter (`~/.local/share/uv/python/cpython-3.11.15-…/bin/python3.11`, reached via the venv symlink, **ad-hoc/linker-signed**) has lost Full Disk Access. Probed live: `Desktop: DENIED`, `Documents: DENIED`, `Downloads: DENIED`, `Library: OK`, `Movies: OK`; `TCC.db` read → "authorization denied". Ad-hoc signatures have no stable designated requirement, so a uv/python replacement drops the grant.
- Not fixable autonomously — requires Ole in System Settings → Privacy & Security → Full Disk Access. Proposal only, nothing wired: guard the `os.listdir` so the run emits its JSON report instead of a traceback, and decide whether a TCC-protected source path is the right design for this job.
- Vault-side state is intact: the 2026-09-15 staged files are in `_Inbox/`; `_Inbox/_Conflicts/` holds **51** files / 560 KB of accumulated leftovers worth a prune.

## Operator-installer: study board verified + TUI session-history fix

- **Verified (not from memory):** the study-tracking board is `lexflow-learning` — "LexFlow Learning Path" 🎓, own SQLite DB at `~/.hermes/kanban/boards/lexflow-learning/kanban.db`, created 2026-09-16 15:48, 5 cards, all `ready`, unassigned, 0 comments: M1 Flask multi-tenancy (`t_d673a2e0`), M2 Postgres RLS (`t_a033743b`), M3 Railway (`t_3bcd618f`), M4 website→CRM wiring (`t_cffefe6a`), M5 deploy hygiene (`t_8785bee4`). Isolated queue — never auto-dispatches dev work. `hermes kanban --board lexflow-learning list` / `stats` both confirm 5 ready.
- Every card carries a concrete deliverable (Obsidian note in `01-Projects/LexFlow/`, annotated diagram, or a personal checklist) — no code changes on this board.
- **Opening it locally:** the board view is the Hermes **desktop app → Kanban tab (Package icon) → board switcher** ("LexFlow Learning Path"). A standalone `hermes dashboard` (tried on port 9157) serves the same Kanban tab but sits behind the dashboard login gate, so the desktop app is the faster path. `screencapture` is unavailable to this shell (no Screen Recording permission), so no visual proof was attached — board content was verified from the SQLite DB + CLI instead.
- **Memory-curator report collected (2 done cards):** `t_a261ac01` — vault logging gaps closed + [[LexFlow-Ecosystem-Index-v3-2026-09-16]] (endpoints re-probed; the new web-site **deploys nowhere**, no remote/no deploy config; only live marketing surface is the legacy LP `poetic-kleicha-28d058.netlify.app`); `t_62db7c26` — [[Hermes-14-Day-Installation-Roadmap-2026-09-16]] at r4 (59–123 skills/profile, 2,131 total; provider split `openrouter` 11 / `google` 8 / `nous` 2; 3 profiles cannot boot; Nous Portal auth stale; Telegram cron delivery failing DNS).
- **Fixed — TUI "loses" chat history:** `display.tui_auto_resume_recent` was `false`, so every TUI launch started a fresh session (visible transcript empty) even though history is fully stored in `state.db`. Set to `true` (profile `operator-installer`); old sessions resumable with `hermes -p operator-installer --continue` / `-r latest`. Today's earlier sessions were intact all along (`20260916_153340_fd5a14`, `..._162925_ab203a`, `..._162126_051a93`).

## LexFlow web-site pre-deploy cross-test (t_cf76a3a3) — 6 PASS / 1 FAIL, not deployed

- Independently cross-tested frontend-developer's build on `feat/heading-align-cta-merge` (`3b9cc41` + `23678b7`) from the local preview on port 8891. Repo untouched, nothing deployed.
- **PASS:** heading integrity 49 pages / 0 issues; one CTA label per language with nav == mid-page (0 mismatches across 45 pages); `data-webhook` values still distinct (`demo-request-nav`, `cta-demo`, `demo-request-hero` present, 31 distinct values tree-wide, no value changed — only label text); hero anchor byte-identical to pre-build (`git diff` = 0 lines touching `demo-request-hero`), still `#intake`; zero `railway.app/admin` anywhere (only the intended public `/login?ws=lexflow` + `/api/contacts`); 61/61 URLs 200 incl. `llms.txt`, `robots.txt`, `sitemap.xml`, `.well-known/openapi.yaml`, both new PNGs, and 48/48 sitemap locs.
- **FAIL (item 7):** FAQ calendar image alt. The build swapped the image to `lexflow-calendar-visual.png` and fixed the static EN alt but kept `data-i18n-alt="alt_team"`; runtime alt therefore resolves to a *team* description in EN ("Law firm team working together"), IT and RU — wrong alt for screen readers, EN a regression. Fix = calendar-specific key in `assets/i18n-ui.js`, not an edit to `alt_team`.
- **Also flagged:** the same build stripped the leading glyphs from CTA/contact links (`▶ Dashboard Preview`, `◉/💬 WhatsApp`, `✉ Email:`) — user-visible, so it needs Ole's localhost review under the design/UX gate.
- Follow-up card `t_6807dd71` created for `frontend-developer-lovable_react` (parent `t_cf76a3a3`); note: [[LexFlow-Web-Site-Cross-Test-PreDeploy-2026-09-16]]; full report + id-correction comment on the card thread.

## SEO/AEO/GEO origin plan for the web-site (t_ee09cd1c) — planned, nothing changed, nothing deployed

- Card from frontend-developer asked this be pencilled into the CRM plan. Produced
  [[LexFlow-Web-Site-SEO-AEO-GEO-Origin-Plan-2026-09-16]] (evidence pack in
  `01-Projects/LexFlow/_from-repos/LexFlow-Web-Site-Origin-Fix-2026-09-16/`). Repo untouched, all runs in scratch copies.
- **Confirmed the ticket:** `ORIGIN = "https://lexflow.example.com"` at `templates/build-lang-sites.py:42`; 54 pages carry it in `canonical`, 51 in `og:url`, 48 in JSON-LD `url`, all 48 sitemap `<loc>`, plus `robots.txt`/`llms.txt`/`ai-plugin.json`/`openapi.yaml` and the "Sign in" `back=` links.
- **New finding 1 — a second bug the ticket didn't have:** 7 English article pages + `cookie-policy.html` declare a canonical that is the *wrong page* (all pointing at `lexflow-article-matter-tracker.html` / `privacy.html`). IT/RU copies of the same articles are correct, so EN and IT/RU disagree about canonical. Those 7 articles would not be indexed.
- **New finding 2 — the ticket's step 1 alone does not fix the site.** I made `ORIGIN` env-driven and rebuilt from a pristine copy: 24 canonicals, 21 og:url and **all 48** JSON-LD urls still on the dead host, because EN pages and JSON-LD are never rewritten by the build. Needed: a self-canonical pass + a build-time origin rotation with a drift guard that aborts the build. Prototype patch (134 lines) applied and verified → 0 / 0 / 0 remaining, EN content intact (data-i18n counts unchanged), hreflang reciprocal.
- **New finding 3 — deploy hygiene, worse than the note said:** a repo-root deploy would serve 141 tracked files of which 53 are not content — including **4 tracked duplicate pages** (`lexflow-index (1).html` … → duplicate content live at a crawlable URL), **15 stale HTML copies under `templates/`** (another homepage, another privacy/terms), internal `.md`/`.csv` (claims register, enrichment report), a tracked `.DS_Store`, plus 6 untracked probe files. Curated `dist/` = 88 files, 0 forbidden (verified). Also no root `index.html` → `/` 404s without a rewrite.
- **Blocked on Ole (§7 of the note):** interim Cloudflare Pages hostname, permission to publish, domain-close timing, `/` vs `/lexflow-index.html`, `ai-plugin.json` keep/drop, and OK to delete the 4 tracked duplicate files. Nothing auto-proceeded.

## Operator-installer: per-employee Kanban + status colours — advisory (Ole question 18:05)

- Ole clarified the `lexflow-learning` board's purpose: **test the CRM**, not study — each employee should get their own Kanban, a new project must not be confused with the rest, and status must move `Started → In Progress → Done` **with colours** on both boards.
- Verified the gap in both systems: Hermes board statuses are **code-fixed** (`hermes_cli/kanban_db.py:89`, 9 values) but **already colour-coded** in `plugins/kanban/dashboard/dist/style.css`; the CRM `templates/kanban.html:678–720` hardcodes 8 legal columns, `Case.status` is a free string, `ownerid`/`assignedto`/`case_no` already exist, `GET /api/cases` filters by status only, priority is coloured but **status is not**, and `crm/routes/saved_views.py` is an already-built per-user/workspace view layer that the board does not use yet. `templates/staff_board.html:264` already renders one column per staff member as a separate page.
- Advice: keep the Hermes board as throwaway prototype; make "own Kanban" an **assignee-scoped view over one shared board** (not a workspace/board per person); make statuses **data** (per-workspace `case_status`: label EN/IT, colour, order, terminal) seeded from the current 8; add RBAC (user = own board only) and run the tenant audit pass. Suggested palette matches the Hermes one (amber/green/red/blue).
- Note: [[LexFlow-Per-Employee-Kanban-Advisory-2026-09-16]] (in `_Inbox/`). No code written, no design changed, nothing deployed — gates: mapping table (status → destination → triggers) + design-gate localhost review first.
- Three decisions put to Ole: D1 scope-vs-separate board, D2 keep 8 statuses + colours vs collapse to 3, D3 Hermes prototype vs CRM spec first.

## Web-site: FAQ calendar alt fix (t_6807dd71) — fixed locally, nothing deployed

- The one FAIL from the pre-deploy cross-test: the FAQ calendar image kept `data-i18n-alt="alt_team"`, so the runtime dictionary overwrote the calendar `alt` with a "team" description in EN/IT/RU (WCAG 1.1.1, EN a regression).
- Added a calendar-specific key `alt_calendar` (en/it/ru) in `assets/i18n-ui.js`, repointed the img, and **regenerated** `/it/` and `/ru/` via `templates/build-lang-sites.py` instead of hand-editing. `alt_team` left untouched.
- Verified in headless Chrome against a local server: correct calendar alt in EN/IT/RU both statically and post-load; a probe page with an empty static alt proved the runtime key resolves (and that `alt_team` still resolves to the team string).
- Landed inside Ole's own commit `2110368` (18:11:46) because he committed the live working tree mid-run; the repo was being edited concurrently (new PNGs, blog-layout tweak). Note: [[LexFlow-Web-Site-FAQ-Calendar-Alt-Fix-2026-09-16]]. No deploy.

## Memory-curator (cron): nightly self-improvement log (Part F) — 2026-09-16

- Appended tonight's entry to [[self-improvement-log]]: both producer jobs identified (`obsidian-staging.sh` 05:12 — 15 staged / 4 quarantined; `obsidian-dedup-check.sh` 05:29 — 1 flagged); `_Inbox/_Conflicts/` backlog 46 → **51**; 2 of today's 5 quarantines are provably byte-identical (basename/size-only dedup logic). No user-facing report — the Sunday weekly job consumes the log.

## Links
- Parent: [[05-Daily-INDEX]]
- Related: [[LexFlow-INDEX]]
- Related: [[LexFlow-Web-Site-SEO-AEO-GEO-Origin-Plan-2026-09-16]]
- Related: [[LexFlow-Web-Site-Cross-Test-PreDeploy-2026-09-16]]
- Related: [[LexFlow-Web-Site-FAQ-Calendar-Alt-Fix-2026-09-16]]
- Related: [[Hermes-14-Day-Installation-Roadmap-2026-09-16]]
- Related: [[LexFlow-Ecosystem-Index-v3-2026-09-16]]
- Related: [[LexFlow-LP-CRM-CTA-Wiring-2026-09-16]]
- Related: [[LexFlow-Legal-Publishing-Blockers]]
