# 2026-09-03

## Night session (while Ole slept) — Try Demo cross-tenant bug FIXED + deployed
- **Bug:** LexFlow landing "Try Demo" → landed in Romanelli workspace (even in Safari used only for LexFlow).
- **Root cause (NOT a data leak):** landing buttons pointed at CRM `/` and `/admin`, which auto-redirect logged-in users by the JWT in domain-scoped localStorage. If a Romanelli token exists in the browser, Try Demo showed Romanelli. Also, `siteMap` in base.html / kanban.html / crm/__init__.py lacked 'lexflow', so "Return to main website" was missing/wrong for LexFlow.
- **Fixes deployed:**
  1. Landing (`LexFlow-landing` repo, commit `6632284`, pushed via branch fix-try-demo → origin/main): all 7 buttons now → `/login?ws=lexflow&back=poetic-kleicha`. Visual-redesign commits (52f1bc6, 01e12aa) kept on hold (visual-redesign-hold branch) — NOT deployed.
  2. CRM (`lexflow_hermes_v1`): commits `4290a68` (base.html + crm/__init__.py) + `3a88e5f` (kanban.html) — siteMap gains 'lexflow' → poetic-kleicha.
- **Verified live:** landing 7× login?ws=lexflow; /kanban siteMap includes lexflow; /login?ws=lexflow = 200; CRM health db: ok.
- **Also delegated** independent audit to backend-dev (deleg_969e20da) — confirmed: navigation bug, not leak; ADDED finding: `/api/admin/users`, `/users/<id>`, `/role`, DELETE, `/stats` were admin-only without workspace filter → any tenant admin could enumerate all users. FIXED in commit `8ae0cd8` (superadmin-only), verified locally (romanelli 403 / superadmin 200 / anon 401).
- Resend email integration fact-checked: code lives in the same Railway project; key invalid → emails not delivered.

## Day session (with Ole)
- **Resend key** (c7da03…) supplied by Ole — REJECTED by Resend ("API key is invalid", both /domains and /emails). Format is a UUID, not `re_…`. Not set in Railway. Awaiting correct `re_…` key from Resend dashboard.
- **Cron daily check created:** `lexflow-try-demo-daily-check` (job `bbde6d0665f4`, daily 08:00, no_agent script `lexflow-try-demo-check.sh`) — checks health, landing buttons (≥7 login?ws=lexflow, 0 bare/admin), /login?ws=lexflow=200, kanban siteMap lexflow, romanelli login=200, anon admin users=401. Silent when OK, reports only on regression → Telegram.
- **Tenant-switch local test:** on /kanban entering different tenant creds → lands in that tenant's workspace, sees ONLY its rows (verified: superadmin=all, pagliano=own, romanelli=own+subs, cl1=own, cl2=own). Isolation holds.
- **Audit rule + checklist created:** `LexFlow-Audit-Checklist.md` (audit know-how) — rule: any tenant-specific fix → audit other tenants for the same bug immediately; append new findings to checklist.

## Afternoon session — Roadmap v2 + working agreements (memory-curator handoff)
- **Roadmap v2 surgical update** (`docs/LexFlow_Agentic_Roadmap.json`, repo `lexflow_hermes_v1`): committed locally, **NOT pushed**.
  - `process_rules.cross_audit`: BEFORE every local test AND before any deploy, operator-installer ↔ backend_dev cross-audit each other's code (operator reviews backend_dev's; backend_dev reviews operator's) → report to user → then deploy. **No exceptions.**
  - `process_rules.implementation_plan_gate`: backend_dev submits an implementation plan BEFORE code ONLY when user explicitly requests it OR operator-installer has a genuine doubt — NOT routine.
  - **T-007 Contacts redesign = two-model split CONFIRMED (Ole)** — see below.
  - **T-017 (NEW, backlog):** `SEAT_LIMIT_DEFAULT` — 5 seats/tenant, DB-configurable, no redeploy.
  - **T-018 (NEW, backlog):** Agentic AI-personalized email automation — confirmed missing, needs its own spec.
- **Working agreements (team):** operator-installer = coordinator/orchestrator (delegates specialist work to backend-dev / frontend-dev / memory-curator, does NOT take all tasks on its own shoulders); cross-audit rule above; implementation-plan gate above.
- **Contact model — two-split CONFIRMED:** Internal staff directory (`firm_team`, workspace-filtered; backend-dev delegated to implement Internal backend — deleg_377ec25e; operator-installer's `firm_team.py` draft = Internal starting point) + Client-Facing (lifecycle/consent/tags; field audit pending user confirmation).
- **Calendar event email notifications (scheme 2/3/4) DEPLOYED** (commit `c81f8af`): client + superadmin both auto-notified on event creation. Test scenario ready: create event in CRM → both emails (client email + Yahoo superadmin). Already logged in inventory ✅.

## Links
[[05-Daily]] · [[LexFlow-INDEX]] · [[LexFlow-Workspace-2026-08-31-Inventory]] · [[LexFlow-Audit-Checklist]]

- Build status: #2 Saved Views + #3 Attachments deployed (08-29); #5 TimelineActivity auto-log deployed (08-29) per decision note → remaining surgical items: #1 Cliente+Studio split, #4 RBAC.
- Follow-ups: dead-button sweep pending; backend-dev audit (deleg_969e20da) outstanding; RESEND_API_KEY invalid → emails blocked → new key needed from Ole.
- SEO/AEO: Romanelli audit → prioritized fix list + AEO FAQ/schema brief; Pagliano LP intake check + lead follow-up.
- Recommended next: pick up #1 Cliente+Studio split (or #4 RBAC given security momentum).

## Links
[[05-Daily]] · [[LexFlow-INDEX]] · [[LexFlow-Workspace-2026-08-31-Inventory]]
