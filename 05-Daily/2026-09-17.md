# 2026-09-17

## [OVERRIDE] DoD S5 debt register — verified and filed (kanban t_3c7158f8, operator-installer)

- Owner invoked deploy authority: `lexflow-site` production was promoted from commit **`bd4b683`** (branch `feat/heading-align-cta-merge`) with **14 of 15** Definition-of-Done checks (§5) failing. Cloudflare deploy `c7b6aec0-66f7-43f0-b419-a918604491b3`.
- Re-derived every claim in the card from the repo and the live site (did not copy the card body): dist **14 MB** ✓, heavy PNGs ✓ (romanelli ×6 908 KB–1.2 MB, blog-team 1.2 MB, chat-avatar 1.1 MB, calendar 704 KB), `bd4b683` is HEAD ✓, **no git remote** ✓, `intakeEndpoint: null` ✓, `/signup` `/demo` `/portal` `/auth/staff` `/admin` `/accessibility` all **404 live** ✓, `/admin` absent from nav ✓, ORIGIN `lexflow-site.pages.dev` with **0** `lexflow.example.com` ✓, robots Sitemap line + `llms.txt` correct live ✓, external Google Fonts on the critical path ✓.
- **3 new findings beyond the card:** (N-1) `hook.example.com` placeholder comment shipped in **45 live pages** — inert (`webhookBase: null`) but it contradicts check #10's "0 placeholders", which was scoped to `lexflow.example.com` only; (N-2) the repo lives at `~/Desktop/projects/services/LEGAL/LEXFLOW Production/LEXFLOW Web-Site` — **violates Repo Root Rule §8.1** and sits in the TCC-protected zone, which is also why item 13 has no remote; (N-3) generated `dist/` is committed to git (`fd2d5f1`).
- Filed: [[LexFlow-Web-Site-Quality-Debt-Register-2026-09-17]] (full 15-item table + fix plans + effort); LexFlow hub updated.
- Card **blocked as needs_input** — the register itself says *do not action without owner sign-off on the rebuild-vs-patch decision*. Options put to Ole: **A** Astro rebuild (§8, ~11 h, gets items 1/2/3 architecturally right), **B** patch the static site (cannot reach items 1–2 in-place), **C** hybrid — patch only the two live-risk items now (#3 image weight / LCP, #10+N-1 placeholder sweep) and schedule the rebuild for the rest. No code touched, no deploy, nothing pushed.

## Hard rules broadcast + daily-status cron repaired (kanban t_228db57d, operator-installer)

- Ole's two new permanent hard rules recorded as **§9 (single source of truth / no duplicate registries)** and the reporting chain + daily status as **§10** in [[_Meta|AGENT_RULES]] → rulebook bumped **v2.2 → v2.3**.
- Propagated to **24 carriers**: 23 profile `SOUL.md` files + `email-digest-agent/profile.yaml` + default `~/.hermes/SOUL.md` (grep-verified count = 24).
- Found two stale copies of the rulebook in `03-Resources/Hermes-Setup-and-MCP/` (v2.0 md, v2 txt) — a **parallel source of truth**; both bannered SUPERSEDED, authority named, nothing deleted.
- Daily status cron was **registered but dead**: it lived in `frontend-developer-lovable_react`, a profile with no running gateway. Removed `2573e20ab61f`; created `cda5f6d4a18a` (`lexflow-daily-status-crm`, 09:00 daily) in `operator-installer`, whose gateway is live (PID 12005, heartbeat 11 s). Next fire 2026-09-18 09:00.
- WARNings handed to Ole: two morning jobs failed today on `ImportError _FULL_ARGS_LOG_BOUND` (import now verified clean — mid-update artefact suspected); Telegram cron delivery failing with `httpx.ConnectError` on 3 jobs; `agent-roster-friday-kpi` missing Nous Portal token; only 2 of 22 profile gateways run.
- Full record: [[Agent-Rules-v2.3-Hard-Rules-Broadcast-2026-09-17]]

## CRM repointed to the new marketing site — root `/` 404 found (kanban t_8586ceed, operator-installer)

- Ole's direct request: repoint the LexFlow CRM's website/home link to the new site. Committed **`6ef25da`** on `lexflow_hermes_v1` (6 files: `crm/__init__.py`, `crm/routes/views.py`, `crm/config.py`, `templates/base.html`, `templates/kanban.html`, the 2026-09-04 UX report). **Not pushed** — a push triggers a Railway production deploy and needs Ole's explicit go.
- The ticket's suggested target `lexflow-site.pages.dev/#hero` is **not usable**: the site root returns **HTTP 404**, and `/lexflow-index.html` is **308**-redirected to the clean URL `/lexflow-index`. Live-verified: `/lexflow-index#hero` = **200** with exactly **1** `id="hero"`. CRM now targets that.
- Fixed a real dead end: the marketing "Sign in" link goes to `/login?ws=lexflow` with no `?back=`, so the CRM login page's "← Torna al sito" was hidden. It now falls back to the tenant's public site (`public_site_url(slug)`, single server-side source of truth).
- Legacy-origin refs = **0** in source and in all 12 rendered routes; `/admin` not referenced from the marketing site. **Retained**: Pagliano's own Netlify LP (different client, live) and the historical roadmap note.
- Follow-up card **`t_b92d36cc`** → `frontend-developer-lovable_react`: choose a root entry point (root `index.html` vs `_redirects`) so the bare domain stops 404-ing. Full record: [[LexFlow-Web-Site-CRM-Repoint-2026-09-17]]

## Site root entry point fixed — `_redirects` 301 (kanban t_b92d36cc, frontend-developer-lovable_react)

- Bare domain `lexflow-site.pages.dev/` was 404-ing (no root entry). Chose **option (b)**: `_redirects` with `/ /lexflow-index 301` (allowlisted in `templates/build-dist.py:29`), NOT a duplicate `index.html` copy (would create a second canonical form). `/lexflow-index` stays the single canonical.
- Committed **`4e526a0`** on `feat/heading-align-cta-merge` (2 files), **not pushed**. Regenerated `dist/` (adds `dist/_redirects`, no `index.html`).
- Local preview via `wrangler pages dev` verified: `/` → **301** → `/lexflow-index` (**200**, exactly **1** `id="hero"`). Handed to review — Ole gives go/no-go (301 vs 302 vs option a) before any deploy. Full record: [[LexFlow-Web-Site-Root-Entry-2026-09-17]]
- **Review verdict: approved (round 1, artifact lens).** Diff read cold then re-executed: `git ls-remote` confirms `4e526a0` unpushed (remote `feat/heading-align-cta-merge` = `0a6f5bd`); my own `python3 templates/build-dist.py` reproduces 89 files / 14.0 MB with `dist/_redirects` (459 B) and **no** `dist/index.html`; wrangler preview on port 8791 gave `/` **301** → `/lexflow-index` **200** (`<title>LexFlow — Legal SaaS for Modern Law Firms</title>`, 1 × `id="hero"`; `it/`+`ru/` also 1 each), `/lexflow-index.html` **308** → clean URL, missing path **404** (custom `404.html`). No second homepage URL in the SEO set: **0** bare-root `<loc>` in the 48-entry sitemap, **0** bare-root canonicals, **0** internal `href="/"`. Only observation (pre-existing, not from this change): the homepage canonical is still the `.html` form, which itself 308s — worth a later tidy-up. Remaining human gate: Ole's go/no-go on **301 vs 302 vs option a**, and the deploy is operator-installer's to run.

## Downloads stager blocked — TCC permission lost (memory-curator, cron 18:00)

- `downloads_stager.py` failed: `PermissionError [Errno 1]` on `os.listdir(~/Downloads)`. No files staged, no manifest written, nothing corrupted (glass half full: it never got to touch `_Inbox`).
- Reproduced 4 ways: script re-run, `ls`, `mdfind`, Finder AppleScript (`-1743`). The vault side is fine — only `~/Downloads` is walled off.
- **Root cause:** TCC binds the Downloads grant to the responsible app's code signature. Hermes.app at `~/.hermes/.../release/mac-arm64/Hermes.app` is **ad-hoc signed, no Team ID**, and its bundle mtime is **2026-09-15 11:44:05** — 2 minutes *after* the last good run (11:42:45, staged 2 files). The rebuild silently revoked the grant; 09-16 and 09-17 have no manifests.
- **Fix proposed (tier-2, NOT actioned):** grant Full Disk Access to that Hermes.app bundle → relaunch → `--dry-run` to verify → `--hours 72` to backfill. Durable fix: stop rebuilding in place / move to a signed build.
- Vault-side duplicate scan in lieu of the Downloads scan: `_Inbox` = **81 files, 1 identical pair** — `hermes-cloud-plan.csv` = `hermes-cloud-plan (1).csv` (1743 B). Archive the `(1)` copy (archive-never-delete), not deleted.
- Full record: [[Incident-Downloads-Stager-TCC-Blocked-2026-09-17]]

## Nightly repo→vault sync + auto-push (memory-curator, 22:42 cron)

- `repo_sync.py --dry-run`: **0 UPD / 0 NEW / 5 identical** — no `--live` run needed (nothing to copy).
- **4 of 5 configured repos skipped again ("no repo")** — 2nd consecutive night (same finding 09-16): `lexflow-crm`, `LexFlow-landing`, `LexFlow-Chatbot`, `aLEXy`. Only `avibe-hindsight` is actually mirrored. Verified real locations: `lexflow-crm` = `~/Desktop/projects/services/LEGAL/LEXFLOW Production/lexflow-crm` @ `6ef25da`; `aLEXy` = `~/Desktop/projects/services/LEGAL/aLEXy` @ `8ea18c2`; `LexFlow-landing` @ `55d5488` + `LexFlow-Chatbot` @ `8a0e8a2` only exist as git repos under `~/projects/LEGAL_backup/`.
- **Not auto-patched** (tier-2, no human to confirm): re-pointing the mirror at `LEXFLOW Production/` widens what gets copied into a GitHub-pushed tree (the GH013 secret-egress failure mode), and the two `LEGAL_backup/` repos may be older than the vault mirrors. Proposal with 3 options: [[Repo-Sync-Stale-Repo-Paths-2026-09-17]].
- Vault autocommit: pushed **`cb9b359..fa316c1`** to `Ole00007/obsidian-vault` main (**28 files** from earlier sessions' notes); `git rev-parse HEAD origin/main` both = `fa316c1` → push landed (the script prints "pushed" without checking the exit code). Secret guard passed.

## Links
- Parent: [[05-Daily-INDEX]]
- Related: [[Incident-Downloads-Stager-TCC-Blocked-2026-09-17]]
- Related: [[Repo-Sync-Stale-Repo-Paths-2026-09-17]]
- Related: [[LexFlow-Web-Site-CRM-Repoint-2026-09-17]]
- Related: [[LexFlow-Web-Site-Quality-Debt-Register-2026-09-17]]
- Related: [[LexFlow-INDEX]]
- Related: [[LexFlow-Web-Site-SEO-AEO-GEO-Origin-Plan-2026-09-16]]
- Related: [[LexFlow-Legal-Publishing-Blockers]]
